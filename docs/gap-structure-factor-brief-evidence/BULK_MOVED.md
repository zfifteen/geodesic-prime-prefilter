# Factor-brief bulk evidence moved

The multiplicative-web dump tree (frozen public_result JSON, band rows, iteration outputs) was moved to:

```text
../prime-gap-structure-artifacts/docs-evidence/multiplicative-web/
```

Paragraph prose under `paragraph-*` remains in this repo. Hashes: sibling `MANIFEST.md`.
