# Paragraph 05 Research Note

## Plain Claim

The fifth panel should state the correction: endpoint structure is meaningful,
but it is not automatically factor recovery. The branch now keeps public
inference, factor recovery, and audit confirmation apart.

## Draft Public Paragraph

A key branch cleanup was semantic rather than decorative. Public endpoint
structure is evidence, but it is not automatically a recovered factor. The
current contract says a row is resolved only when public inference finds at
least one actual factor endpoint and a separate downstream audit confirms that
fact. Sidecar measurements, blocker notes, and survivor states remain useful
research evidence, but they do not count as factor recovery.

## Evidence Status

```text
implementation_status = audit script separates public structure and factor verdict
audit_status = tests enforce at least one actual factor for factor_found
invalidated_rule = endpoint class alone is not a factor result
```

## Infographic Direction

Three gates: public structure, factor endpoint, audit confirmation. Only after
the second gate does the row become a factor result.

