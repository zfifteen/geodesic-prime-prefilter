# Paragraph 01 Source Excerpts

## Source: Cryptology Chapter Route Map

Path: `research/06-cryptology-rsa/README.md`

Line evidence:

- `1-6`: names the chapter object as RSA, modulus-link, semiprime,
  reciprocal closure, structural certificates, and unresolved survivor states.
- `22-31`: states the required frame and separates public structural inference
  from classical factorization tools.
- `33-35`: states that no RSA-scale resolver theorem is claimed.
- `92-102`: preserves the rule that unresolved survivor states and public
  endpoint classes are not factorization results unless audit reports
  `factor_found = true`.

Use for paragraph 01:

The opening must say the branch is about recovering a hidden factor from public
structure around a product. It must not imply a completed universal resolver.

## Source: RSA Experiments Route Map

Path: `research/06-cryptology-rsa/experiments/README.md`

Line evidence:

- `7-14`: identifies the live solver, next solver entrypoint, and data ladder.
- `39-49`: defines current terminology and the live target:
  `public N -> reciprocal PGSPG certificate pair -> factor or unresolved`.
- `51-52`: states that sidecars remain diagnostics unless promoted by a public
  theorem.

Use for paragraph 01:

The external opening can say the live target is public product to factor or
unresolved state. It should avoid internal abbreviations in final prose, so the
term behind `PGSPG certificate pair` needs to be explained as public interval
structure before any name is used.

## Source: Endpoint Structure Law

Path: `research/06-cryptology-rsa/docs/endpoint_structure_law.md`

Line evidence:

- `3-10`: strongest supported claim: RSA moduli expose deterministic endpoint
  structure, and the artifact is a public endpoint-class resolver rather than
  an audit trick or candidate search.
- `14-24`: starts from public `N`, the square-root center, and the previous
  public endpoint.
- `42-56`: transports through `floor(N / x)` and builds the opposite-side
  public certificate.
- `64-78`: strict reciprocal closure condition.
- `80-113`: deadline-signature correction condition.

Use for paragraph 01:

The first paragraph should introduce the public object, the hidden factor
endpoints, and the research question. It should not introduce strict closure,
deadline correction, or certificate vocabulary yet.

## Source: Live Runner README

Path: `research/06-cryptology-rsa/experiments/live-solver/rsa-v2/README.md`

Line evidence:

- `12-24`: describes the live runner as a reciprocal endpoint-chain resolver.
- `26-34`: records invalid solver shapes and clarifies that the square root is
  an orientation coordinate, not a candidate chamber or factor-distance limit.
- `186-187`: says the factorizer calls the generator's chamber-reset
  certificate as a read-only local state adapter.
- `191-207`: records current output surfaces and downstream audit separation.

Use for paragraph 01:

The public opening should not start from square-root search. It should start
from the visible product and the ordered public structure that can be inspected
without knowing the factors.

