# Grok Part One Completion Prompt

Your previous Part One run hit the CLI max-turn limit after writing the probe and raw outputs.

Do not modify:

```text
reciprocal_shadow_v2_public_selector_probe_grok.py
output/summary.json
output/certificate.jsonl
output/runtime_residue_crt_log.jsonl
output/summary.md
```

Write only these missing documents in the same Part One folder:

```text
self_checklist.md
grok_execution_notes.md
```

Use the existing source and output files as the basis. State the result plainly:

- structural wins are `0/20`;
- both controls are empty at certificate layer on all 20 cases;
- final classification under the V2 table is `invalidated_result`;
- this invalidates the V2 ranking hypothesis on the first 20-case surface if the independent audit accepts the implementation.

The checklist must answer the 14 V2 contract items from `residue_certificate_v2_public_selector_contract.html`.

The execution notes must separate:

- hypothesis under test;
- measured result;
- implementation status;
- audit status, pending Codex audit;
- invalidated or unresolved state;
- next research move.

Do not add new code. Do not rerun the experiment. Do not write outside the Part One folder.
