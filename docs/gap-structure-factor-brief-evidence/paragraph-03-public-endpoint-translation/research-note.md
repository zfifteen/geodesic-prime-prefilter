# Paragraph 03 Research Note

## Plain Claim

The third panel should explain how the branch carries the proved gap idea into
the product setting. A public product is placed on a public ruler. Endpoint
states are built on one side, transported through division by the product, and
compared to endpoint states on the other side.

## Draft Public Paragraph

The factor branch translates the gap foundation into a public endpoint chain
around a product. Begin with the visible product and the ordered marks near
it. Choose a public endpoint on one side, carry that position through division
by the product, and inspect the endpoint structure that appears on the other
side. When both sides close against each other, the runner emits a public
endpoint class. When the public structure fails the closure tests, the runner
stops with an unresolved structural state.

## Evidence Status

```text
implementation_status = endpoint-chain runner exists
measured_status = deterministic endpoint structure appears on committed rows
theorem_status = no universal factor theorem claimed here
invalidated_rule = square root is orientation, not a factor-distance chamber
```

## Infographic Direction

Show a lower endpoint chain, an arrow through `floor(N / x)`, and an upper
endpoint chain. Make closure look like two hooks meeting.

