# Paragraph 03 Evidence: Public Endpoint Translation

## Public Claim To Support

The recent branch translates the prime-gap foundation into a public endpoint
chain around a semiprime: start from public interval structure, transport it
through division by the public product, and look for reciprocal closure.

## Supporting Evidence

- `research/06-cryptology-rsa/docs/endpoint_structure_law.md` states the
  strongest supported claim: RSA moduli expose deterministic endpoint
  structure.
- The same document defines the active law using public data:
  a square-root center, previous public endpoint, reset certificate,
  reciprocal floor transport, strict reset closure, and deadline-signature
  correction.
- It explicitly states that the endpoint-chain walk does not use fixed
  candidate radius, retry ladders, product closure, divisibility, `gcd`,
  primality APIs, hidden factors, or audit endpoints.
- `research/06-cryptology-rsa/experiments/live-solver/rsa-v2/README.md`
  describes the factorizer as calling the generator's chamber-reset certificate
  as a read-only local state adapter.

## Status Boundary

- Implementation status: a public endpoint-chain runner exists.
- Measured result: endpoint structure is exposed on committed rungs.
- Audit result: exact factor status is checked after public inference.
- Theorem status: no universal RSA-scale factor theorem is claimed.

## Infographic Concept

Show `N` above a square-root center. A lower public endpoint chain walks left;
reciprocal floor arrows jump to an upper endpoint chain. Closed pairs are drawn
as matched hooks; unresolved pairs stop at a broken hook.

