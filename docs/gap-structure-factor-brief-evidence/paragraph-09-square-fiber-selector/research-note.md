# Paragraph 09 Research Note

## Plain Claim

The ninth panel should state the latest positive measured signal: public
product residue groups the candidate lanes into three fibers, and a public gate
co-landing test selects the current survivor lanes inside those fibers.

## Draft Public Paragraph

The latest positive signal is more specific. The visible product residue
groups twelve same-phase candidate lanes into three public square fibers, four
lanes per fiber. The product residue alone does not choose the surviving lane,
but a second public co-landing test does on the current matrix: one fiber
selects no survivor, and the other two select exactly the two survivor lanes.
This is a measured selector signal, not yet a universal selector theorem.

## Evidence Status

```text
measured_status = selector matches current survivor lanes
theorem_status = hypothesis not proved
factor_status = factor_found false
next_target = derive co-landing gates from public neighboring-gap rows
```

## Infographic Direction

Show twelve lanes collapsing into three boxes labeled `37`, `97`, and `157`.
Then mark the selected lane in `97` and `157`, none in `37`.

