# Paragraph 10 Evidence: Current Status And Next Target

## Public Claim To Support

The honest external status is strong progress without a completed universal
proof: the branch has isolated a narrow public selector problem, verified
current live-factor wins on committed small rungs, preserved unresolved rows,
and defined the next proof target.

## Supporting Evidence

- `research/06-cryptology-rsa/experiments/live-solver/rsa-v2/output/audit_results.csv`
  gives the current committed live-runner audit surface:
  40-bit true, 50-bit false, 64-bit true.
- `REMAINING_BRIDGE_INVENTORY.md` states:
  `theorem_status = hypothesis_not_proved`,
  `measured_status = one_remaining_bridge`, and
  `remaining_bridge = public_left_residue_gate_lower_twin_conjunction`.
- `ROUND19_SHADOW_SURFACE_DEBRIEF.md` preserves an invalidated shortcut:
  broad shadow signal true, selector success false.
- `codex_round21_public_square_fiber_root_selector/summary.json` states the
  strongest latest selector finding and gives the next context:
  promote the selector from lane-matrix measurement to a public candidate
  procedure by deriving co-landing gates directly from public neighboring-gap
  rows.
- Recent branch arc:
  - May 12: square-tail and near-root lemmas sharpened the local gap
    foundation.
  - May 13-15: live RSA v2 runner and endpoint-structure semantics were
    corrected.
  - May 16-17: the proof search reduced the gap-compatibility problem to the
    public-left-residue/lower-twin bridge and then found the square-fiber
    selector signal.

## Status Boundary

- Strongest supported branch claim: the proof search has isolated a narrow
  deterministic bridge from public product structure to candidate survivor
  lanes.
- Implementation status: live runner exists and has current committed outputs.
- Measured status: latest selector matches current survivor lanes.
- Audit status: 40-bit and 64-bit committed rungs pass factor audit; 50-bit
  remains unresolved.
- Theorem status: universal factor proof is not complete.

## Infographic Concept

End with a status board: three columns labeled Built, Measured, Missing.
Built contains the live runner and status separation. Measured contains the
one-bridge reduction and square-fiber selector. Missing contains the public
proof that co-landing gates force the survivor lane in general.

