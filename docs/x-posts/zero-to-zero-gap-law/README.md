We reduced a noisy prime-gap compatibility search to one compact measured rule: public selected defect zero plus endpoint transport defect zero excludes 45,337 endpoint cells with zero exact falsifications. Save this one if you are tracking the factor-location branch, because the signal collapsed to a small, checkable structure.

![Zero to zero overview](01-zero-to-zero-overview.png)

---

Start with the public side. A composite number sits inside a gap between two consecutive primes. Inside that public gap there is a selected position computed from the gap's own divisor structure. In the clean case, the composite lands exactly at that selected position. That is the public zero, and it is the part to watch.

![Public selected zero](02-public-selected-zero.png)

---

Now look at the factor side. Each hidden prime factor has neighboring gaps around it. The clean endpoint condition says the right-opening behavior after the two endpoints has no transport defect. In residue language, both endpoint slots avoid 1 and 23, and at least one endpoint slot hits 7, 13, or 19. This is the piece worth stress-testing.

![Endpoint residue rule](03-endpoint-residue-rule.png)

---

The contrast test is what makes this sharp. Endpoint balance alone leaks when the composite is not at the selected public point. At the selected point, endpoint transport defect zero gives 45,337 excluded cells and zero falsifications. Move away from that public point and the same endpoint idea starts leaking. This matrix is the shareable evidence.

![Contrast matrix](04-contrast-matrix.png)

---

This is a measured law candidate, not a finished factor recovery algorithm. Broad representation search is done for this layer. The proof target is now narrow: explain why public selected defect zero stabilizes supported prior absence exactly at endpoint transport defect zero. Follow the next step: turn the measured kernel into the theorem.

![Proof target](05-proof-target.png)
