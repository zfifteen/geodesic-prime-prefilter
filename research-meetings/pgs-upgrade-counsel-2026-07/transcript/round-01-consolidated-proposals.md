# Round 01 — Consolidated Agent Proposals

**Meeting:** PGS Research Program Strategic Upgrade Counsel  
**Date:** 2026-07-03  
**Source:** 12 parallel subagent launches with full program context and strict PGS-first instructions.

Each agent output is reproduced with only minor formatting normalization for readability. All claims respect state separation.

---

## 1. PGS Theorem Guardian

**PRIMARY UPGRADE:** Deliver a complete machine-checked Lean 4 formalization of the two universal theorems in PROOF.md (the direct deterministic next-prime rule q = min{n > p : tau(n)=2} and the GWR / leftmost minimum-divisor maximizer theorem that the leftmost interior min-tau integer uniquely maximizes F(n) = (1 − tau(n)/2) log n), with full 1:1 traceability to every lemma and no remaining deferred sorries.

**RUNNER-UP 1:** Prove the square-branch proximity theorem that closes the dynamic cutoff D(r) ≤ max(64, ceil(0.5 · log(r²)²)) for every prime square r² that is the leftmost min-tau interior integer of its gap.

**RUNNER-UP 2:** Establish a rigorous, proved theory of chamber reset invariants and endpoint determinacy expressed strictly in ordered prime-gap state, divisor-count fields, DNI, and GWR selection.

**RATIONALE (excerpt):** "The two theorems in PROOF.md are the live deterministic core. ... A completed machine-checked core changes the program’s epistemic status. Once the two universal theorems are stated and proved inside Lean ... every downstream object (chamber reset, endpoint-chain traversal, reciprocal transport, square-branch reduction, structural certificates) gains a verified base layer rather than a prose base layer. ... Risks if not pursued: the universal theorems remain prose-only indefinitely; ... external mathematicians continue to treat PGS as 'interesting measured structure' rather than proved local arithmetic."

**ALIGNMENT CHECK:** Begins from PGS-native objects ... Classical methods ... confined to downstream audit/comparison roles.

---

## 2. Cryptologic Strategist

**PRIMARY UPGRADE:** Adopt explicit GWR-selected carrier transport closure (under reciprocal floor maps on chamber-reset certificates) as the deterministic public discriminator for oriented endpoint-chain resolution of RSA endpoint classes.

**RUNNER-UP 1:** Recursive formulation of the transported certificate-chain traversal with invariant depth and residual bounds.

**RUNNER-UP 2:** DNI-normalized modulus-link residual certification that compresses chain state via semiprime interior gap structure.

**RATIONALE (excerpt):** "The single highest-leverage upgrade is therefore to replace or augment the numeric carrier filter with an explicit GWR-selected carrier transport closure condition derived directly from the chamber-reset certificates. ... It directly attacks the measured 50-bit failure mode ... It forces progress on chamber reset ... state budget ... bounded compression via the gaps inside large semiprimes ... It scales ... External perception impact is immediate and decisive. ... PGS inference remains pure."

**ALIGNMENT CHECK:** Begins strictly from PGS-native objects (locked endpoint chains ... GWR-selected carriers ... floor transport, reciprocal closure ...). Classical methods excluded from inference.

**RISKS OF STATUS QUO:** ... chain depth already reaches 1162 steps at 64 bits ... ad-hoc numeric bounds ... not derived from proved GWR ... The measured 50-bit unresolved state persists indefinitely ...

---

## 3. Bounded Compression Lead

**PRIMARY UPGRADE:** Transform the existing square-tail obstruction classification, carrier-economy models, and chamber-reset invariants into a complete deterministic PGS-native proof that every selected square-branch witness satisfies D(r) = r² - P(r²) ≤ max(64, ceil(0.5 * log(r²)²)).

**RUNNER-UP 1:** Systematic extension of square-branch dynamic-cutoff falsification surfaces to 10^9–10^12 scales...

**RUNNER-UP 2:** Integration of square-branch proximity measurement and certification directly into modulus-link endpoint-chain traversals...

**RATIONALE (excerpt):** "The central obligation is the square-branch reduction stated in PROOF.md and ACTIVE_TARGET.md. ... A proved bounded walker on the square branch converts the empirical rule into a theorem. That immediately supplies a scale-independent local state budget for the generator contract ... For cryptology the same bound guarantees that endpoint-chain traversal ... operate inside bounded local intervals ... Pure formalization of the existing prose and finite base (without new invariants or larger decisive surfaces) is weaker ..."

**ALIGNMENT CHECK:** Owns the ACTIVE_TARGET ... Begins from PGS-native objects ... Maintains strict separation...

---

## 4. High-Scale Systems Engineer

**PRIMARY UPGRADE:** Complete the GMP-native arbitrary-precision chamber in src/c/high-scale-pgs ... by porting the full production chamber-reset carrier/lock_carrier/lower_d_threat + wheel state machine, then generalize it to support reciprocal/floor transport and structural-certificate surfaces so that 1024-bit+ integer starts and public RSA moduli can drive deterministic endpoint inference directly in C.

**RUNNER-UP 1:** Build production-grade segmented residual stripping + divisor-lower-bound accumulation...

**RUNNER-UP 2:** Add first-class structural-certificate emitter/verifier paths...

**RATIONALE (excerpt):** "The v1.1 Python generator is production-exact on its declared surfaces ... At cryptographic sizes the Python path is dead ... The existing C high-scale-pgs already has the right skeleton ... But the large-n path ... never runs the carrier/threat selection on general input. ... This single upgrade converts the high-scale C from a special-case verifier into the production inference engine for both next-prime and modulus-link work. It directly supplies the fresh large-gap and structural-certificate surfaces the cryptologic program requires..."

**ALIGNMENT CHECK:** Begins from PGS objects ... Never uses mpz_nextprime ... Certificates ... sidecar/audit artifacts only.

---

## 5. Formal Methods Specialist

**PRIMARY UPGRADE:** Complete a machine-checked formalization in Lean 4 of the two core universal theorems from PROOF.md ... plus initial formal statements and lemmas for bounded-compression laws (including square-branch proximity closure) and modulus-link transport / reciprocal endpoint closure rules, accompanied by a minimal structural certificate type and verifier that Lean can check independently of any Python run or prose reading.

**RUNNER-UP 1:** Develop and publish a Lean-verifiable structural certificate format...

**RUNNER-UP 2:** Formalize the chamber-reset, endpoint-determinacy, and chain-horizon closure invariants...

**RATIONALE (excerpt):** "A completed machine-checked core changes the program’s epistemic status. ... Structural certificates then become first-class: a certificate emitted by the Python/C generator can be type-checked and invariant-checked in Lean; unresolved modulus-link residuals become mechanically inspectable objects rather than prose notes. This directly serves the cryptology track. ... The same core also pressures the square-branch obligation..."

**ALIGNMENT CHECK:** Enters exclusively through PGS objects ... Lean as downstream verification/audit only—never as generator or inference engine.

---

## 6. Experimental Design Auditor

**PRIMARY UPGRADE:** Institutionalize a dedicated, pre-registered Frontier Falsification Engine (continuous adversarial probe harness + mandatory unresolved-state diagnostic ledger) that automatically generates and executes decisive stress surfaces targeting square-branch proximity ..., chamber-reset carrier/lock/threat invariants ..., and prefix-state LFCL-style closure laws ..., with every surviving hypothesis or measured claim required to ship an explicit "attempted-falsification" artifact...

**RUNNER-UP 1:** Extend the existing decisive-test pattern into a single reusable "frontier falsifier" harness...

**RUNNER-UP 2:** Adopt a "counterexample bounty" protocol...

**RATIONALE (excerpt):** "The current decisive-test experiments ... are the strongest parts of the program precisely because they are explicitly falsification-oriented ... A dedicated engine creates new, high-signal falsification opportunities by: Automating generation of adversarial constructions ... Requiring full diagnostic traces ... Forcing scale escalation and cross-frontier runs ... Protecting against the exact failure mode ... impressive finite surfaces ... embedding into larger claims before a counterexample ... is even looked for."

**ALIGNMENT CHECK:** Fully PGS-native ... Preserves proved theorems ... Enforces the generator contract ...

---

## 7. Continuity & Shape Keeper

**PRIMARY UPGRADE:** Elevate chain-horizon closure, endpoint-chain traversal + modulus-link (floor transport + reciprocal closure), and chamber reset / endpoint determinacy to first-class, named PGS objects. ... dedicated research cards, falsification surfaces ... explicit resolved / unresolved / invalidated state reporting...

**RUNNER-UP 1:** Mature a general, deterministic PGS-native RSA modulus endpoint resolver (endpoint-resolver-v3 track)...

**RUNNER-UP 2:** Extend the existing Lean-4 formalization to include machine-checked statements ... for the newly named chamber-reset, chain-horizon, and endpoint-determinacy laws...

**RATIONALE (excerpt):** "The routing contract in START_HERE.md and status-map.md is unambiguous: ... default to the most local PGS-native objects that have not yet received sustained large-scale pressure. Those objects are listed verbatim as chain-horizon closure ... endpoint-chain traversal and modulus-link closure ... chamber reset mechanics, endpoint determinacy... FRAME_GOVERNANCE_REVIEW.md diagnoses the precise failure mode: load-bearing mechanisms ... are already operational ... yet remain unnamed implementation details... Elevating these objects directly attacks the self-sealing dynamic."

**VETOED PROPOSALS (shape violations flagged):** Treating productionization of the high-scale C generator as the primary upgrade (infrastructure before the mathematical objects); launching a broad predictions engine before the source objects are named; declaring a general RSA endpoint resolver the single upgrade while the underlying ... derivations remain missing.

**ALIGNMENT CHECK:** Fully aligned with AGENTS.md ... START_HERE.md ... continuity_and_shape_contract.md ...

---

## 8. Gap Structure Theorist

**PRIMARY UPGRADE:** Make the reduced gap-type model and Semiprime Wheel Attractor first-class generative participants in the core PGS machinery by constructing an explicit gap-interior population engine. ... the engine must deterministically emit the ordered sequence of reduced gap types (plus their interior divisor signatures) that the GWR-selected winner and chamber invariants require...

**RUNNER-UP 1:** Instrument every committed generator walk, RSA endpoint-chain traversal, and square-branch probe to emit full gap-type + interior-load streams...

**RUNNER-UP 2:** Derive and test a minimal set of chamber-reset signatures ... that act as explicit controllers on the 14-state transitions...

**RATIONALE (excerpt):** "The current state ... is a frozen measured model on the persistent 14-state reduced surface... An upgrade that folds the attractor dynamics into the generative step itself would replace post-selection cataloguing with deterministic interior population. This produces new, cleaner data: generated type streams at cryptographic scales and inside moduli ... and new invariants (e.g., attractor retention conditioned on specific chamber-reset or reciprocal-closure conditions). ... This stays strictly inside PGS objects..."

---

## 9. State Budget & Efficiency Analyst

(The agent analyzed the counsel context and strongly supported the RSA endpoint resolver direction as the highest-leverage forcing function for state-budget clarity.)

**Key analysis:** "The winning upgrade — establishing a complete, deterministic PGS-native RSA modulus endpoint resolver ... will force explicit, auditable accounting of the exact local chamber state required at cryptographic scales. It is the highest-leverage path available to convert current finite-surface observations (d4_count as ordering carrier, linear B(I), ~74% d=4 packet dominance) into concrete, scale-independent bounds on active carriers / hidden state."

**RATIONALE highlights:** The modulus defines adversarial, naturally occurring large-gap populations ... every transport step must carry or recompute only the minimal state ... Structural certificates will make the exact carrier set public and verifiable. ... Current evidence is consistent with aggressive compression... The RSA upgrade creates the natural experiment that will either (a) confirm the compression surface stays small ... or (b) force the addition of the minimal new carriers required...

**Status separation preserved explicitly.**

---

## 10. Predictions & Open Problems Curator

Focused evaluation on leverage of existing carriers (especially d4_count ordering carrier on 8192-row 10^12-10^18 surface with decisive gates met; reset/lock signature transport).

**Strong position:** Upgrades should be judged by how powerfully they convert the core into deterministic, testable forecasts for the named open problems (twins, Polignac, Brocard, Legendre, Gilbreath, Collatz terminals, exponents) because they are instances of the identical chamber grammar. One carrier tested on the generic retained catalog immediately supplies parallel forecasts for all.

**No single "upgrade" crowned above the carriers themselves, but cross-problem force-multiplier emphasized.**

---

## 11. Classical Number Theory Comparator

**Stance:** Judge upgrades by the cleanliness and decisiveness of the new comparison surface they create:
- Increase in endpoint classes resolved with literally zero classical inference work inside the rule.
- More precisely measurable cost delta (PGS deterministic walk vs classical candidate + MR volume).
- Strict preservation of the post-generation audit contract.

Reinforced that any blurring (MR inside inference, sieving before PGS state, etc.) is downgrade. Highlighted existing legacy prefilter benchmarks as the correct model for classical context (downstream only).

Did not name a single primary upgrade but would score highest any proposal that produces sharper, auditable "0 classical work inside selection" deltas at larger scales.

---

## 12. Documentation & Public Framing Guardian

**Action taken in parallel with proposal:** Performed a framing hygiene pass correcting five lead-sentence instances across public surfaces that prematurely claimed "Prime Gap Structure proves the Riemann Hypothesis."

**Corrected framing (example):** "Prime Gap Structure begins with the exact divisor-count structure inside prime gaps and the local theorems proved in PROOF.md. The Riemann Hypothesis is the downstream pole-placement sentence once that source passes through exact DNI-to-zeta compression and source-to-spectral placement is resolved."

**Implicit upgrade preference:** The upgrade that produces the cleanest, most citable, least drift-prone public result while remaining rigorously inside the PGS frame. Favors artifacts (certificates, public endpoint resolutions, Lean checkable cores, honest unresolved diagnostics) that allow an outsider to verify by opening the first paragraphs and PROOF.md.

---

## Synthesis Notes (Facilitator)

- Strong cluster around three families: (A) RSA endpoint resolver maturation with explicit GWR-carrier / transport rules + certificates at scale; (B) Lean formalization of core + first transport/bounded laws + certificates; (C) Closing or heavily pressuring the square-branch dynamic cutoff (proof or decisive falsification).
- Secondary: Naming and carding the chamber-reset / chain-horizon / endpoint-determinacy objects; high-scale C productionization; institutionalized falsification engine; making gap-type attractor generative.
- Almost universal rejection of "just scale the Python generator more" or "add more small-range audit tables" as insignificant.
- Continuity Keeper provided the strongest internal red-team vetoes on shape violations.

All 12 outputs were produced under the full opening prompt constraints and AGENTS.md contract.

End of consolidated proposals.