# Residual Honesty Pressure Relay — CHARTER

**Status:** draft under dual review (grok + nie) — principal 2026-07-16: iterate until both accept, then mark live  
**Opened from:** nie proposal in `#Prime-Gap-Structure` (2026-07-16); lead drafted CHARTER  
**Not a theorem.** Not program-level verified language. Measured / unresolved only under exact pins.

## Purpose

Recurring multi-agent pressure on the still-live **50-bit / joint-cell** residual track, with optional parallel **Lean axiom burn-down**. Square-branch hourly is **not** the collaborative centerpiece (LaunchAgent disabled 2026-07-16).

Each cycle either:

- **(A)** expands public residual geometry so false mutual-close stays blocked, or  
- **(B)** leaves the pin honestly unresolved with a sharper named residual  

…never by widening first-tail or classical smuggling (`gcd`, `isprime`, window widen, product closure as inference).

## Default pin

| Field | Value |
| --- | --- |
| Pin id | `rsa_v2_50bit_static_001` |
| Residual class | `unresolved_by_joint_cell_C1T2L1` (decision residual under joint cell; see rsa-v3 taxonomy) |
| Continuity note | `research/00-index/continuity/notes/ACTIVE_GOAL_50bit_residual_discriminator.md` |
| Optional ladder | 64-bit live runner, or one new public mutual-close **true** pin |

No free-range residual-class serial minting on fixed util maxima.

## Public fields only (PGS-native)

Allowed pressure surface:

- GWR carrier  
- first-tail  
- lock  
- dual-gap `D`  
- residual vector `R`  
- pinch `S`  
- chamber certificates  

Forbidden as inference: classical primality/factor gates, first-tail widen, audit-as-proof.

## ADVANCE gate (hard)

One activation = one claim. **ADVANCE** only if at least one of:

1. New executed regime/fixture with signature change, or  
2. Residual subclass migration with a kill command that can fail, or  
3. Proof-pressure artifact that closes a named Lean axiom  

**NO_DELTA by policy:**

- pytest green alone  
- same-seven-chamber renorm without residual change  
- audit corroboration of a proved law rebranded as breakthrough  

## Roles (collab)

| Who | Duty |
| --- | --- |
| grok (lead) | Goal, fan-out, synthesis, DONE with zero peer `@tags` |
| hermes | Probe / ledger / fixture implementation |
| feynman | Mechanism kill-check: what would make C1T2L1 appear on a true close? |
| nie | Intensity / honesty gate: is ADVANCE inflated? is residual name honest? |
| agy | Adversarial read of claim language |

Principal opens with **only** `@grok`. Lead fans out. Short epochs; clean close-out.

## Cadence

- **Primary relay:** 2–3 cycles per week (not every 4 hours / Heavy-16).  
- **Lean track (slower):** about weekly, one axiom or one placement milestone; `#print axioms` as falsifier.  
- Channel memo only when ADVANCE fires or honest NO_DELTA with a kill observation worth sharing.

## Ship surface

Per cycle:

1. FINDINGS block (claim + status labels)  
2. Taxonomy update if residual name changes  
3. Regression pytest  

Optional continuity touch: `research/00-index/continuity/` notes if the pin or residual name moves.

## Explicit non-goals (schedule)

- Offset-540 / RC48–RC53 style residual minting on fixed util maxima  
- Multi-@ every peer on every hop  
- “Verified / validated residual” without residual-family `10^18` surface (claim-language rule only; does not downgrade theorems)

## First cycle seed (when principal wants live)

Principal (or lead after explicit go):

> `@grok` Open residual-honesty pressure relay. First cycle: pressure C1T2L1 expand-or-honest-NO_DELTA only.

Then lead assigns hermes/feynman/nie/agy under the roles above.

## Re-enable square-branch

Repo template remains at `scripts/launchd/com.velocityworks.pgs-hourly-advance.plist`. Loaded agent is disabled as `.plist.disabled` under `~/Library/LaunchAgents/`. Re-enable is a separate ops act, not part of this relay.
