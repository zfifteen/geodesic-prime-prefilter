# Execution collab charter: full PGS × sieve research exploration

**Date:** 2026-07-15  
**Epoch:** pgs-sieve-execution-2026-07  
**Lead:** grok  
**Peers:** hermes, agy, claude  
**Status:** done (lead closed after R0 package + kill adjudication + FINDINGS)  

**Parent design package (complete):** `../` (D1 contract, D4 bridge, KILL_SHAPES, DIRECTIONS)

## Shared goal

**Execute** a full research exploration of the five directions — not design-only. Primary path is D1 measured atlas (R0 then R1), kill-shape adjudication on real rows, D4 linkage to measured compression, D2 pilot sample if D1 clean, D3/D5 scoped or deferred with evidence. Produce FINDINGS with status labels and a clear residual for the principal.

## Done bar (do not stop short)

1. **D1 R0 measured** — rows + summary under `execution/d1_atlas/` for p ≤ 10^6 (smoke R0_smoke optional). Language: measured on R only; no verified.  
2. **D1 analysis** — bounded vs control offset/compression tables; empty-interior rate; H246 slice.  
3. **Kill-shape adjudication** — apply KILL_SHAPES K1* to measured R0; pass / redesign / kill.  
4. **D4 measured link** — one page tying measured compression ratios to the two-bounds bridge (no implication inflation).  
5. **D2 pilot OR explicit defer with reason** after D1 adjudication.  
6. **D3/D5** — either one falsification pin or deferred with definitional fence from KILL_SHAPES.  
7. **FINDINGS.md** synthesis + single residual next step for principal.  

## Hard contracts

- PGS-first interior via `gwr_next_gap_profile` (divisor field). Primes catalog = input only.  
- No Super-Signal. No theorem inflation. No RSA/RH.  
- No verified/validated without 10^18 surface for this claim family.  

## Artifacts

`experiments/pgs-sieve-research-directions-collab-2026-07/execution/`
