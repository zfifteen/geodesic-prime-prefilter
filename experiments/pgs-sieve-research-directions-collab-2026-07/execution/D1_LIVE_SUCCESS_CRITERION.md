# D1 live success criterion (post R0 adjudication)

**Status:** execution rule after K1-b/c/e redesign. Measured language only on named regimes.

## Primary

1. Stratify consecutive gaps by **raw gap width** (fixed bands and/or continuous summaries).  
2. Controls: **within log10(p) bin**, small = ≤p25, large = ≥p75 (quartile mode).  
3. Report offset and compression distributions with exact n; empty-interior rate.  
4. On small-gap cohort, subclass witnesses: left-endpoint (offset=1), right-endpoint (offset=gap−1), genuine interior.  
5. Classical H∈{246,600,1000} may appear as **labels only**, not primary cohorts, until max gap exceeds those scales.

## Secondary (compression claim)

Do not claim H-band compression superiority over controls while UBC floor 64 dominates. Revisit compression narrative only when `0.5 log(q)²` is competitive with 64 for a non-trivial fraction of the sample (R1+).

## Forbidden

Sieve/isprime choosing `w`. Verified language without 10¹⁸ surface. Super-Signal. Twin/Zhang equivalence claims.
