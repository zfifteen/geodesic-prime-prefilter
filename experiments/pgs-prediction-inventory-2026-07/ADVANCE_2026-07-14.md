# Prediction inventory advance (2026-07-14)

**Sources:** Hermes `INVENTORY.md`; Claude falsifiability pressure; Agy top-three schedule.  
**Authority:** `PROOF.md` still controls theorem labels. Adversarial proof-hygiene notes do **not** silently demote theorems.

## Adopted advances

### 1. Canonical inventory

Use `INVENTORY.md` as the killable master list (T/F/I/H stacks + non-predictions). Soft-wording corrections from Hermes are adopted:

- Next-prime is the operator claim; “empty interior” is definitional, not a separate deep theorem.
- Dual-gap D hold is **measured on fixtures**; D as universal law is **hypothesis**.
- NLSC corollary is **theorem**; 10^18 zeros are **audit corroboration**.
- Program-level **verified** needs executed 10^8–10^18 decade ladder in the same package.

### 2. Residual honesty rewrites (Claude → adopt as H1′–H3′)

**H1′ (constant-neighborhood honesty):** On the golden 50-bit pin, for fixed dual-gap form `max(C1, floor(α(g_lo+g_up)))` with `(C1,α)` in a named neighborhood around the fitted gate (e.g. C1∈[10,40], α∈[0.8,2.0]), the decision residual stays `unresolved_by_first_tail_misalignment` under unchanged first-tail window. Closing the pin requires new geometry or a derived (not retuned) gate.

**H2′ (no close by monotone relaxation):** No monotone loosening of boundD / first-tail constants alone yields an honest structural close without new public law or classical smuggle. Such a “green” commit **falsifies residual honesty**.

**H3′ (constants predicted to break or be derived):** Free constants are hypothesis gates. Either they break at some scale with honest residuals, or they are replaced by a derivation. Larger-surface revalidation alone does **not** promote them to law.

**H4′ (Super-Signal):** No reparameterization restores universal zero-FP on surfaces that include known class-B counterexamples. Narrowing scope to hide CEs is not restoration.

### 3. Classical-only confusion list (not PGS predictions)

Admissibility / k-tuples; Rowland gcd engines; Zhang–Maynard gap≤246 i.o.; average `d(n)~log n`; Cramér gap-size conjecture as if UBC; Bertrand computational checks as PGS success.

### 4. Schedule drivers (Agy — adopted)

| # | Driver | Pass/fail | After green |
| --- | --- | --- | --- |
| 1 | 50-bit residual honesty pin | Decision residual first-tail; no false endpoint | **measured** on fixture; D law still **hypothesis** |
| 2 | Generator decade ladder 10^8..10^18 | Exact q; no classical selection | **measured** / implementation surface (enables verified language only with this package) |
| 3 | Constant-gaming sweep on 50-bit | Pure constant retune must not “close” | **hypothesis** hardened if sweep holds |

**Reject schedule:** twin/constellation campaign, Rowland engine, sieve-first generator, RH proof sprint.

### 5. Proof-hygiene note (Claude) — open pressure, not status flip

Claude argues UBC/PSP analytic closure should be treated as still under refereeing pressure and that A1/A3/A6 are near-definitional. Program policy: **`PROOF.md` still labels UBC/PSP as theorem.** Next work may *audit the proof spine* and Lean gaps without rewriting theorem status in chat or inventory unless a human-approved proof-promotion/demotion process runs. Inventory rows T3/T4 stay **theorem** with kill = true counterexample under hypotheses; finite ladders remain **corroboration**, not re-proof.

## Next execution order

1. H1′ + H2′ + I6: residual honesty + anti-admission (live research).  
2. I3 + I1 + I4: ladder + stream purity + audit separation (implementation integrity).  
3. T3/T4 audit corroboration only on new regimes (no re-litigation).  
4. Optional: proof-spine / Lean hygiene for UBC/PSP (proof-support mode).

## Inventory merge (Hermes, same day)

`INVENTORY.md` now includes H1′–H4′, Schedule drivers 1–3 with commands, F1/F2 SHA-256 pins, and finite-check ≠ re-proof notes under T3/T4. Theorem labels for UBC/PSP unchanged.
