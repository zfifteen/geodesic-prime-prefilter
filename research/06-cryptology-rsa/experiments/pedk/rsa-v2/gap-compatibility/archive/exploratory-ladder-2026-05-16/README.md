# Exploratory Ladder Archive

This directory preserves the earlier representation-search ladder from the
PEDK gap compatibility branch.

It contains:

```text
archived_result_notes = 35
archived_experiment_scripts = 26
```

These files are not deleted because they record useful falsifications,
representation failures, and intermediate evidence. They were moved out of the
active `core-evidence/` root because the current law target has become much
sharper:

```text
public at-winner condition
    selects right-following factor endpoint residue classes
        Rres=o2|o4 and Rres=o4|o4
    which exclude exact endpoint-pair cells
```

Use this archive for research archaeology, not as the current starting point.
The current starting point is:

```text
../../core-evidence/README.md
../../core-evidence/CURRENT_STATE_OF_GAP_COMPATIBILITY_LAW.md
../../core-evidence/PUBLIC_GRAMMAR_FORWARD_BOUNDARY_LAW_PROFILE.md
```

Archived scripts may depend on their original same-directory import layout.
If one needs to be rerun, restore it deliberately or update its imports as a
separate research action.
