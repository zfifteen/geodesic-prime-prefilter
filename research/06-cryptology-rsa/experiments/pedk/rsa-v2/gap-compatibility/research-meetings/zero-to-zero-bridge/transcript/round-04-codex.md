# Round 4 Codex Answer

Change the Rung 2 status line.

The best wording is:

```text
Status line: measured bridge; unresolved transport necessity.
```

That keeps the evidence and the missing proof separated.

The measured bridge is:

```text
public selected load = 4
right endpoint load = 4
prior-absent supported endpoint cell
    -> 0 observed exact endpoint-pair falsifications
```

The unresolved step is narrower:

```text
load equality forces any preserving reentry to use the lower-factor
immediate-left carrier.
```

The terminal-twin description is measured, but the necessity claim is not yet
proved. So we should not label the whole rung as merely unresolved. We should
label the measured surface and the exact proof gap.

Everything else in the ladder is acceptable as the meeting deliverable.
