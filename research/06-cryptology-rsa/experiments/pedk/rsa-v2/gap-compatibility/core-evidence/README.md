# Core Gap Compatibility Evidence

This folder is the active evidence surface for the PEDK gap compatibility
hypothesis.

The current measured object is compact:

```text
public at-winner condition
    selects right-following factor endpoint residue classes
        Rres=o2|o4 and Rres=o4|o4
    which exclude exact endpoint-pair cells
```

Across the strict-forward windows measured so far, those two clean
right-following classes have no exact endpoint-pair falsifications:

```text
Rres=o2|o4 = 0 / 22509
Rres=o4|o4 = 0 / 8109
combined    = 0 / 30618
```

All observed right-gated falsifications fall outside those two classes.

## Current Reading Path

Read these files first:

```text
CURRENT_STATE_OF_GAP_COMPATIBILITY_LAW.md
GAP_COMPATIBILITY_HYPOTHESIS.md
SIMPLE_RIGHT_RESIDUE_INVARIANT.md
PUBLIC_GRAMMAR_FORWARD_BOUNDARY_LAW_PROFILE.md
PUBLIC_GRAMMAR_DIRECTIONAL_BOUNDARY_GATE_RESULTS.md
PUBLIC_GRAMMAR_JOINT_ENDPOINT_PAIR_RIGHT_BOUNDARY_RESULTS.md
PUBLIC_GRAMMAR_HYBRID_ENDPOINT_PAIR_SURFACE_RESULTS.md
PUBLIC_GRAMMAR_RIGHT_BOUNDARY_SURFACE_RESULTS.md
```

The older exploratory ladder has been moved out of the active root:

```text
../archive/exploratory-ladder-2026-05-16/
```

Those files are preserved as research history. They include useful
falsifications and failed representations, but they are no longer the main
entrypoint for the law.

## Active Scripts

The active executable spine is:

```text
first_gap_compatibility_check.py
enriched_multiplication_map_corpus.py
right_boundary_compatibility_surface.py
hybrid_endpoint_pair_surface.py
joint_endpoint_pair_right_boundary_surface.py
directional_boundary_gate_surface.py
forward_boundary_law_profile.py
simple_invariant_probe.py
```

Supporting helpers remain in this folder because the active scripts import
them directly:

```text
endpoint_pair_family_profile.py
gwr_relative_all_o6_boundary.py
intermediate_projection_surface.py
multiplication_map_law_surface.py
public_feature_all_o6_boundary.py
public_grammar_pivot.py
slot_factor_public_quotient_test.py
```

## Current Status

```text
theorem_status = hypothesis_not_proved
inference_status = not_live_pedk_inference
measured_status = strong_forward_stable_compatibility_signal
active_surface = public at-winner condition -> right-following endpoint maximum
active_candidate_law = max(right endpoint residue)=o4 excludes exact endpoint-pair cells
```

## Regeneration

The current boundary-law profile is generated by:

```text
python3 forward_boundary_law_profile.py
python3 simple_invariant_probe.py
```

Generated output is written under:

```text
output/forward_boundary_law_profile/
```

The `output/` tree is ignored. It is scratch evidence output, not committed
source.
