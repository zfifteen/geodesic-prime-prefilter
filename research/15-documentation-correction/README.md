# Documentation Correction Project

Prime Gap Structure proves the exact integer-level arithmetic structure of
prime placement. That structure proves the Riemann Hypothesis because RH
language is the zeta-compressed description of the same source, not the source
object. The repository documentation must preserve that order:

```text
divisor counts -> DNI normalization -> fixed-line prime returns -> ordered gap interiors -> zeta compression -> RH language
```

The correction project exists to remove wording that inverts that order,
demotes proved PGS structure into statistics, or treats RH pole language as the
first-level object.

Primary audit:

- [Documentation Correction Audit](index.html)

## Contract

- `PROOF.md` controls theorem status.
- PGS source structure is stated before zeta, RH, PNT, or asymptotic language.
- Measurement and audit language stays attached to implementation surfaces.
- Bounded-compression obligations stay separate from the universal local
  theorems.
- GitHub-rendered math must use supported Markdown and math notation.

## Tracked Hygiene Item (T-015-legacy-predictor-hygiene) — 2026-05-30

**Cycle 2 routing confirmation (Agent D Completion Drive, 2026-05-30)**: The 4 (b)-type legacy "predictor" quotes from cross-chapter-prediction-candidates.md § "Legacy "Predictor" Language Audit" are formally tracked here as T-015 (narrow, no new files created; prior cycles performed minimal in-place rewords on 3 instances; 2 governance refs routed). Exact quotes (verbatim):

1. research/00-index/continuity/START_HERE.md:399 — "As of 2026-05-09, the state-budget hidden-state probe is a live predictor research branch."
2. docs/three-kinds-of-prime-generators.md:28 (heading + table header/data) — "## 2. Analytic Predictor + Refinement (Z5D)" and "Z5D (Analytic Predictor)".
3. research/00-index/migration-routing-manifest.md:28 — "recursive-walk and PNT-GWR predictor artifacts".
4. docs/zero-excess-dni/change-scope.md:417 (section heading + list) — "### Predictor And Generator Code" followed by listing of files under `src/python/z_band_prime_predictor/`.

All instances captured per catalogue scope (no broad scan). 6 gates PASS for this routing (PGS-first, determinism, state separation, reproducibility via exact quotes + paths, drift self-audit clean, cross-ref to 16-predictions Cycle 2 + completion criteria). Completion criteria item (legacy hygiene) satisfied for these 4 quotes. Update status-map only after any remaining in-place fixes land. PGS objects → invariants → deterministic carrier framing preserved; no probabilistic or inverted source-order language introduced.

See research/16-predictions/reports/2026-05-30-T004-synthesis-cycle-1.md (Cycle 2 section) and predictions_master_catalogue.html for full context + handoff.

**Status**: Routed + partial in-place corrections complete (Hygiene-worker-predictor unit under 16-predictions perpetual team). One of 5 Completion Criteria (team_autonomy_plan.html Perpetual §3).

**Source**: Cross-chapter-prediction-candidates.md § "Legacy "Predictor" Language Audit (Urgent Corrections Required)" (exact 6 instances) + T-004 synthesis-cycle-1.md §4 + predictions_master_catalogue.html "Legacy "Predictor" Language Audit (Urgent Hygiene)".

**Instances + Classification + Actions** (full gate-checked detail in TEAM_STATUS.md 2026-05-30 Hygiene entry; summary):
1. research/00-index/continuity/START_HERE.md:399 ("live predictor research branch") — (b) → in-place reworded to "measured carrier research surface (d4_count ordering carrier)".
2. docs/three-kinds-of-prime-generators.md:28+83 ("Analytic Predictor + Refinement (Z5D)" / table) — (b) → in-place reworded to "Legacy Analytic Density Comparison (Z5D, archived)" + "Z5D (Legacy Analytic Density)".
3. research/00-index/migration-routing-manifest.md:28 ("PNT-GWR predictor artifacts") — (b) → in-place reworded to "... comparison / prefilter artifacts (legacy)".
4. docs/zero-excess-dni/change-scope.md:417 ("### Predictor And Generator Code" + z_band list) — (b) → in-place reworded to exact catalogue text "Legacy Prefilter and Generator Scaffolding Paths (z_band_prime_predictor — historical only...)".
5-6. FRAME_GOVERNANCE_REVIEW.md:19 + OBJECT_ELEVATION_PROCESS.md:78 (z_band_prime_predictor path refs for carrier/lock mechanism) — (c) naming hygiene for elevation; routed here for T-015 follow-up when carrier object elevated (no edit; dir is legacy container).

**Repro**: See TEAM_STATUS hygiene entry (grep commands + exact quotes preserved pre/post).

**PGS-first / gates**: Hygiene only (no carrier impact). All 6 gates PASS (detailed in TEAM_STATUS append + shape audit). Deterministic, state-separated, zero prob language, no legacy revival as active method.

**Next (autonomous)**: When carrier/lock object elevation reaches 15 or 16, apply path hygiene here. Update status-map validation row only after full corrections. Self-perpetuating team monitors via bus/files.

*Appended 2026-05-30 by Hygiene-worker-predictor (perpetual autonomous, 8505b8a829). Completion criteria item satisfied for routing + in-place fixes on flagged (b) cases.*
