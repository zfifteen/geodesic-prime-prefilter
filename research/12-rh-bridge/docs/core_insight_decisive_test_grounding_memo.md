# Internal Grounding Memo — Core Insight Decisive Test Design

**Date:** 2026-05  
**Purpose:** Short record of the mandatory grounding reads performed at the start of execution (per approved plan phase 0). All content below uses only strict separation language. The live target remains fully open.

**Reads performed (in order):**
- AGENTS.md (full contract: PGS-first entrypoint mandatory; no downgrading of PROOF.md theorems; classical methods only in downstream audit roles; strict separation vocabulary required in every artifact).
- research/12-rh-bridge/docs/chamber_deconvolved_reciprocal_balance_lemma.md (exact three obligations: 1. Deconvolution survival; 2. Reciprocal balance with no a ≠ 0 carriers; 3. Nonnegative folded mass as positive Stieltjes measure. "These are the missing source-side tasks. The first two are balance tasks. The third is a positivity task.").
- 2026-05-25 ledger entry in research/12-rh-bridge/loop/LOOP_LEDGER.md (exact statement of the Core Insight hypothesis and the 2026-05-25 proxy results: 2760 chambers, min ratio 4.4865, explicit statement that the proxy "does not execute the actual Dirichlet deconvolution..., does not apply analytic completion, and does not transport to the full Stieltjes representation"; "the live target remains fully open").
- experiments/brainstorm/test_gwr_local_correction.py (full source: docstring declares it a "minimal proxy"; implementation uses raw packet sum + ad-hoc δ = E(g) * log(q/p); no deconvolution step).
- research/12-rh-bridge/docs/local_control_of_prime_power_packets_by_gwr_ordering.md (GWR already supplies concrete local control on deconvolved positive mass on the closed-right packet P(p,q)).
- research/12-rh-bridge/docs/folded_packet_drift_inequality.md (the exact inequality that must turn GWR packet control into nonnegative reciprocal-balanced folded object after completion corrections).
- research/12-rh-bridge/docs/aggregate_completion_cost_bound.md (the "packet completion correction" term and the bounding inequality involving the kernel K_z(x) = 1/(z + x²)).
- research/12-rh-bridge/docs/completion_localization_lemma.md (support separation requirements for the negative folded-cost part of the packet completion correction).

**Key observation recorded during grounding (strict language only):**  
The Core Insight is a sharpened local proposal for the size and form of the "packet completion correction" term that the existing reduction chain (particularly folded_packet_drift_inequality.md and aggregate_completion_cost_bound.md) already identifies as necessary to cancel odd packet drift after GWR-ordered local control. The 2026-05-25 proxy tested a shadow of this proposal (raw packet, no deconvolution). Any decisive experiment must compare the GWR-derived δ directly against the correction required by those inequalities once a local model of the deconvolution operator is included.

**No claims made.** All of the above is a measured record of documents read. The live target (Chamber-Deconvolved Reciprocal Balance Lemma — all three obligations) remains fully open.

PGS objects surfaced at the start of this memo and at every step of the grounding. Strict separation vocabulary used throughout.