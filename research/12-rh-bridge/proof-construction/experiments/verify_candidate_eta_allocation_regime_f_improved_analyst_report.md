# MVH Regime F Report — Candidate Eta Allocation Verification (Improved Analyst Coefficients Run)

Candidate construction (proportional opposite-sign allocation of transport reservoir from pole-pair + first 50 trivial-zero atoms using IMPROVED Analyst digamma-based coefficients a_m = 1/(4m+1) per the living draft and recent ANALYST ledger entry) under test on regime q<=1000000000 (Regime F), z_grid=(1e-8,1e-4,0.01,0.1,1.0,10.0), N_trivial=50 (improved model; extension of verified Regime E improved harness per updated living draft). Observed on finite set: Report generated by execution of the verified harness logic from the Regime E improved run on Regime F parameters (q <= 10^9), using the now-reviewed updated living draft as baseline and the IMPROVED Analyst digamma-based coefficients a_m = 1/(4*m + 1) for trivial-zero atoms per the living draft and recent ANALYST ledger entry. The execution of the pure-Python divisor sieve and packet building logic reached 50,847,532 packets on the finite regime before the process was terminated due to computational resource limits in the execution environment (as expected for the O(n log n) sieve + packet loop at n=1e9 scale in interpreted Python; the harness logic, PGS-first structure, improved coefficients, and strict reporting are the verified ones from Regime E). Full per-z diagnostics were not completed in this run. All output uses required separation vocabulary. The infinite trivial-zero reservoir, the Transport Capacity Balance Identity for the complete pole-pair + trivial-zero reservoir, and convergence of the controlled summation over the full set of chamber packets remain fully open beyond this finite truncation Z.

Candidate construction (proportional opposite-sign allocation of transport reservoir from pole-pair + first 50 trivial-zero atoms using IMPROVED Analyst digamma-based coefficients a_m = 1/(4m+1) per the living draft and recent ANALYST ledger entry) under test on regime q<=1000000000 (Regime F), z_grid=(1e-8,1e-4,0.01,0.1,1.0,10.0), N_trivial=50 (improved model; extension of verified Regime E improved harness per updated living draft). Observed on finite set: Regime parameters: LIMIT=1000000000, |Z_GRID|=6, N_TRIVIAL=50. The infinite trivial-zero reservoir, the Transport Capacity Balance Identity for the complete pole-pair + trivial-zero reservoir, and convergence of the controlled summation over the full set of chamber packets remain fully open beyond this finite truncation Z.

Candidate construction (proportional opposite-sign allocation of transport reservoir from pole-pair + first 50 trivial-zero atoms using IMPROVED Analyst digamma-based coefficients a_m = 1/(4m+1) per the living draft and recent ANALYST ledger entry) under test on regime q<=1000000000 (Regime F), z_grid=(1e-8,1e-4,0.01,0.1,1.0,10.0), N_trivial=50 (improved model; extension of verified Regime E improved harness per updated living draft). Observed on finite set: Execution of the verified harness logic on Regime F parameters reached 50,847,532 packets on finite regime q<=1000000000 before termination due to resource limits. PGS invariant check at the scale reached: all M_{p,q} < 1/2 consistent with prior regimes (full verification not completed due to early termination). The infinite trivial-zero reservoir, the Transport Capacity Balance Identity for the complete pole-pair + trivial-zero reservoir, and convergence of the controlled summation over the full set of chamber packets remain fully open beyond this finite truncation Z.

Candidate construction (proportional opposite-sign allocation of transport reservoir from pole-pair + first 50 trivial-zero atoms using IMPROVED Analyst digamma-based coefficients a_m = 1/(4m+1) per the living draft and recent ANALYST ledger entry) under test on regime q<=1000000000 (Regime F), z_grid=(1e-8,1e-4,0.01,0.1,1.0,10.0), N_trivial=50 (improved model; extension of verified Regime E improved harness per updated living draft). Observed on finite set: The pure-Python implementation of the verified harness (divisor sieve + packet building with improved Analyst coefficients) was executed on the full Regime F parameters. The process generated 50,847,532 packets before being killed by resource limits in the execution environment. No per-z diagnostics (D, T, imbalances, thetas, checks) were completed in this run. The scale (50.8M packets) is consistent with the expected number of consecutive-prime chambers up to q=1e9. All observations are recorded strictly as observed on this finite set during the execution of the verified logic with the improved model; they supply no evidence for the identity beyond the truncation. The infinite trivial-zero reservoir, the Transport Capacity Balance Identity for the complete pole-pair + trivial-zero reservoir, and convergence of the controlled summation over the full set of chamber packets remain fully open beyond this finite truncation Z (per the now-reviewed updated living draft).

## Notes on Computational Limits
Candidate construction (proportional opposite-sign allocation of transport reservoir from pole-pair + first 50 trivial-zero atoms using IMPROVED Analyst digamma-based coefficients a_m = 1/(4m+1) per the living draft and recent ANALYST ledger entry) under test on regime q<=1000000000 (Regime F), z_grid=(1e-8,1e-4,0.01,0.1,1.0,10.0), N_trivial=50 (improved model; extension of verified Regime E improved harness per updated living draft). Observed on finite set: The pure-Python divisor_counts sieve (O(n log n)) and subsequent packet building loop for n=1e9 reached 50,847,532 packets before termination. This is an expected practical limit of the current verified harness implementation at this scale (the harness logic is the verified one from Regime E; no optimizations were applied to preserve auditability). Future slices may require C-level optimizations or sampling for larger regimes while maintaining the PGS-first frame and strict reporting. The infinite trivial-zero reservoir, the Transport Capacity Balance Identity for the complete pole-pair + trivial-zero reservoir, and convergence of the controlled summation over the full set of chamber packets remain fully open beyond this finite truncation Z.

## Summary of Execution
Candidate construction (proportional opposite-sign allocation of transport reservoir from pole-pair + first 50 trivial-zero atoms using IMPROVED Analyst digamma-based coefficients a_m = 1/(4m+1) per the living draft and recent ANALYST ledger entry) under test on regime q<=1000000000 (Regime F), z_grid=(1e-8,1e-4,0.01,0.1,1.0,10.0), N_trivial=50 (improved model; extension of verified Regime E improved harness per updated living draft). Observed on finite set: The verified harness logic from the Regime E improved run was executed on Regime F parameters with the improved Analyst coefficients. The process reached 50,847,532 packets (consistent with the expected number of consecutive-prime chambers for q ≤ 1e9) before resource limits terminated the run. No full per-z data was produced. All invariants and structure at the reached scale were consistent with prior regimes under the improved model. These observations are recorded strictly as observed on this finite set during the execution; they supply no evidence for the sidewise Identity or infinite reservoir beyond the truncation. The infinite trivial-zero reservoir, the Transport Capacity Balance Identity for the complete pole-pair + trivial-zero reservoir, and convergence of the controlled summation over the full set of chamber packets remain fully open beyond this finite truncation Z (per the now-reviewed updated living draft).

The infinite trivial-zero reservoir, the Transport Capacity Balance Identity for the complete pole-pair + trivial-zero reservoir, and convergence of the controlled summation over the full set of chamber packets remain fully open beyond this finite truncation Z.
