# DNI-to-Zeta Compression Research

## Center Of Gravity

The current project center is the DNI/PGS Prime-Structure Program:

```text
PGS is the source. RH is the compressed zeta-language description.
```

The controlling correction before the Off-Axis Pair Carrier review was:

```text
The arithmetic structure inside prime gaps gives the exact integer-level
source that zeta later records.
```

The current proof-state correction is sharper:

```text
the exact integer-level structure identifies the source and the DNI ratio;
it does not yet prove source-to-spectral placement of the nontrivial poles
```

The program starts from integer-level divisor-count structure: chambers,
endpoint returns, selected integers, and DNI normalization. RH pole or zero
placement is the classical coordinate language that appears after zeta
compression. It is not the object that defines PGS and it is not the judge of
the source arithmetic.

The controlling direction is:

```text
divisor counts -> DNI/GWR prime placement -> zeta compression -> RH pole-placement language
```

## Do Not Misread This

- Invalid: PGS is merely a zeta-side restatement of RH.
- Valid: PGS is the integer-level mechanism; RH is downstream pole-placement
  language.
- Unresolved: the final pole-placement sentence still requires the
  chamber-load spectral-centering theorem recorded in
  `docs/off_axis_pair_carrier_lemma_resolution.md`.
- Valid: PGS already defines the integer-level structure; classical pole
  placement is the downstream reading of that structure.

Start here:

- `docs/prime-structure-program/index.html`
- `docs/dni_rh_bridge.md`
- `docs/off_axis_pair_carrier_lemma_resolution.md`
- `docs/chamber_load_spectral_centering_resolution.md`
- `docs/chamber_deconvolved_reciprocal_balance_lemma.md`
- `docs/deconvolution_sign_regularity_from_chamber_ordering.md`
- `docs/prime_power_packet_dominance_under_chamber_ordering.md`
- `docs/local_control_of_prime_power_packets_by_gwr_ordering.md`
- `docs/folded_packet_drift_inequality.md`
- `docs/aggregate_completion_cost_bound.md`
- `docs/kernel_weighted_prime_power_packet_estimate.md`
- `docs/packet_drift_weighted_average_lemma.md`
- `docs/combined_reduction_with_weighted_average_lemma.md`
- `docs/completion_transport_radius_lower_bound.md`
- `docs/completion_localization_lemma.md`
- `docs/completion_term_decomposition_for_localization.md`
- `docs/zero_radius_completion_constant_assignment.md`
- `docs/global_completion_negative_cost_conditions.md`
- `docs/exact_completion_assembly_theorem.md`
- `docs/exact_completion_assembly_strategy.md`
- `docs/transport_reservoir_allocation_rule.md`
- `docs/transport_capacity_balance_identity.md`
- `docs/pole_trivial_capacity_decomposition.md`
- `docs/pole_pair_odd_capacity.md`
- `docs/trivial_zero_reservoir_capacity.md`
- `docs/trivial_zero_capacity_regularization_candidate.md`
- `docs/negative_drift_pole_capacity_condition.md`
- `docs/negative_drift_class_decomposition.md`
- `docs/left_dominant_negative_drift_condition.md`
- `docs/left_excess_drift_envelope.md`
- `docs/one_sided_regularization_lower_production_failure.md`
- `docs/symmetric_trivial_zero_capacity_requirements.md`
- `docs/alpha_split_normalization_candidates.md`
- `docs/trivial_zero_finite_part_split_obstruction.md`
- `docs/net_finite_part_packet_sum_requirement.md`
- `docs/existing_global_machinery_survey_for_packet_balance.md`
- `docs/chamber_centered_von_mangoldt_finite_part_strategy.md`
- `docs/endpoint_log_gap_summatory_law_candidate.md`
- `docs/reciprocal_endpoint_occupancy_theorem_candidate.md`
- `docs/reciprocal_square_gap_energy_lemma.md`
- `docs/reciprocal_gap_energy_bound_obstruction.md`
- `docs/reciprocal_gap_energy_proof_strategy.md`
- `docs/zero_excess_return_time_tail_strategy.md`
- `docs/positive_excess_persistence_estimate_candidate.md`
- `docs/persistence_energy_inequality_strategy.md`
- `docs/age_divisor_recurrence_potential_candidate.md`
- `docs/gwr_to_age_transfer_obstruction.md`
- `docs/chamber_age_energy_upper_invariant_candidate.md`
- `docs/age_divisor_zero_excess_square_moment_reduction.md`
- `docs/divisor_channel_age_orthogonality_strategy.md`
- `docs/divisor_channel_age_orthogonality_energy_bound.md`
- `docs/endpoint_modulus_age_recurrence_strategy.md`
- `docs/modulus_link_endpoint_lattice_closure_strategy.md`
- `docs/endpoint_lattice_closure_invariant_obstruction.md`
- `docs/zero_excess_return_square_moment_consolidation.md`
- `docs/zero_excess_square_moment_attack_consolidation.md`
- `docs/even_channel_age_recurrence_attack.md`
- `docs/odd_offset_persistence_estimate_strategy.md`
- `docs/odd_divisor_covering_tail_lcm_strategy.md`

## Object

DNI-to-zeta compression research records the exact bridge, RH-facing
explanatory notes, and the small test surface for the bridge helper code.

## Contents

- `docs/`: prime-structure program, bridge note, and RH-facing research prose.
- `assets/`: RH explanation image sequences.
- `tests/`: chapter-local tests for bridge helper behavior.

## Status

This chapter records the exact zeta compression of the PGS source, the
PGS-first prime-structure program, and the classical RH translation surface. It
does not change root PGS theorem status. The Off-Axis Pair Carrier Lemma is
invalidated as stated; the remaining target is chamber-load spectral centering.

The Off-Axis Pair Carrier Lemma route was audited on 2026-05-24 and
invalidated as stated. A paired off-axis zero does not require an extra
source-side carrier beyond `tau`, `H`, `D`, `K`, and `R`; it would be a global
singularity of the same continued quotient. The current bridge target is the
source-to-spectral placement theorem recorded in
`docs/off_axis_pair_carrier_lemma_resolution.md`.

The follow-up Chamber-Load Spectral Centering pass found that the raw
chamber-wise centering route is false in the first nonempty chamber, and that
the viable target begins only after Dirichlet deconvolution by `D`, where the
quotient coefficients are `Lambda(n)`. The live narrow target is now the
Chamber-Deconvolved Reciprocal Balance Lemma recorded in
`docs/chamber_deconvolved_reciprocal_balance_lemma.md`.

The first local sign-regularity consequence is recorded in
`docs/deconvolution_sign_regularity_from_chamber_ordering.md`: deconvolved
chamber mass is nonnegative and supported at endpoint primes plus interior
prime powers, while GWR sees that support only when the selected minimum is a
prime power.

The next local arithmetic obligation is recorded in
`docs/prime_power_packet_dominance_under_chamber_ordering.md`: the
GWR-ordered chamber must control the endpoint and interior prime-power packet
well enough to produce nonnegative reciprocal-balanced folded mass after
completion.

The current diagnostic bound is recorded in
`docs/local_control_of_prime_power_packets_by_gwr_ordering.md`: GWR ordering
does prove a selector-to-packet coefficient envelope and pointwise endpoint
dominance for interior prime powers, while the completed folded packet
inequality remains the smallest additional local theorem obligation.

The folded target is stated in
`docs/folded_packet_drift_inequality.md`: the completion correction must cancel
the odd packet drift and its negative even folded cost must be no larger than
the endpoint plus interior prime-power folded reserve.

The aggregate-cost reduction is recorded in
`docs/aggregate_completion_cost_bound.md`: existing GWR packet controls give
an explicit upper bound on negative folded completion cost, but closing it
requires a kernel-weighted prime-power packet estimate inside each chamber.

The kernel-weighted estimate is reduced by
`docs/packet_drift_weighted_average_lemma.md`: because `J_z(x)=xK_z(x)` and
the packet weights are nonnegative, `|D|/R` is bounded by the maximum centered
packet excursion. The combined target is now recorded in
`docs/combined_reduction_with_weighted_average_lemma.md`: the remaining
completion-side obligation is `rho_{p,q}(z) >= max |x_n|` for the same packet.

The completion-side candidate is recorded in
`docs/completion_transport_radius_lower_bound.md`: the required radius bound
follows from support separation for the negative folded-cost part of the
packet completion correction.

The formal support condition is stated in
`docs/completion_localization_lemma.md`: the negative folded-cost measure for
the packet completion correction must be supported at centered coordinates
with `|x| >= max |x_n|`.

The completion terms are decomposed in
`docs/completion_term_decomposition_for_localization.md`: pole and trivial-zero
supports are outside every packet excursion by Bertrand's theorem; the only
possible localization violators are zero-radius regularization/main constants.

The zero-radius constants are assigned in
`docs/zero_radius_completion_constant_assignment.md`: since `J_z(0)=0`, they
have no odd transport capacity and must not enter the packetwise negative
folded-cost measure.

The global consistency conditions are recorded in
`docs/global_completion_negative_cost_conditions.md`: the packetwise completion
corrections must assemble exactly, with no negative zero-radius leakage and
with summable localized negative folded cost.

The formal completion-side theorem target is recorded in
`docs/exact_completion_assembly_theorem.md`: the standard completion terms must
admit a packetwise decomposition compatible with the explicit formula and the
global negative-cost conditions.

The candidate proof route is recorded in
`docs/exact_completion_assembly_strategy.md`: allocate pole and trivial-zero
transport reservoirs packetwise while keeping zero-radius constants out of
negative folded cost.

The candidate allocation rule is recorded in
`docs/transport_reservoir_allocation_rule.md`: distribute opposite-sign
transport capacity proportionally to packet drift, reducing the proof to a
transport capacity balance identity.

The sidewise capacity target is stated in
`docs/transport_capacity_balance_identity.md`: the pole-pair and trivial-zero
reservoir must have negative and positive odd capacities exactly matching the
opposite signed packet drift demands.

The capacity identity is decomposed in
`docs/pole_trivial_capacity_decomposition.md`: the finite pole-pair capacity
is explicit, while the infinite trivial-zero capacity balance is the main
analytic burden.

The finite pole-pair contribution is computed in
`docs/pole_pair_odd_capacity.md`: the pole pair supplies equal positive and
negative odd capacities `1/(2(z+1/4))`.

The infinite trivial-zero reservoir is analyzed in
`docs/trivial_zero_reservoir_capacity.md`: raw trivial-zero atoms supply
one-sided negative odd capacity with logarithmic divergence, so a regularized
sidewise capacity definition is required.

The first regularization candidate is recorded in
`docs/trivial_zero_capacity_regularization_candidate.md`: keep the trivial-zero
reservoir one-sided via a finite part, making the positive-capacity side a
sharp pole-only condition.

The pole-only positive-side condition is stated in
`docs/negative_drift_pole_capacity_condition.md`: the total negative packet
drift must equal `1/(2(z+1/4))`.

The negative-drift side is decomposed in
`docs/negative_drift_class_decomposition.md`: endpoint-only packets never
contribute, and negative drift requires left-of-center interior prime-power
dominance.

The left-dominant class is isolated in
`docs/left_dominant_negative_drift_condition.md`: its contribution is the
aggregate excess of left interior prime-power drift over endpoint drift.

The left-excess drift envelope is recorded in
`docs/left_excess_drift_envelope.md`: GWR ordering gives an upper
compatibility bound for left-dominant pole capacity, but not a lower
production theorem for the finite pole capacity.

The one-sided regularization decision is recorded in
`docs/one_sided_regularization_lower_production_failure.md`: current GWR
selector envelopes cannot produce the required pole-capacity lower bound or
exact identity, so the live bridge route must use a completion representation
with positive trivial-zero transport capacity unless a new independent global
identity is proved.

The symmetric completion-capacity requirements are recorded in
`docs/symmetric_trivial_zero_capacity_requirements.md`: positive trivial-zero
transport capacity requires a sign-regularized split of the existing
trivial-zero atoms, with exact assembly, controlled summation, no negative
zero-radius leakage, and a canonical normalization.

Candidate normalization rules for the symmetric split are recorded in
`docs/alpha_split_normalization_candidates.md`: deterministic demand-matched
rules can supply positive capacity, but the live obstruction is a canonical
gamma finite-part normalization compatible with the sidewise transport
identity.

The centered finite-part obstruction is recorded in
`docs/trivial_zero_finite_part_split_obstruction.md`: the gamma finite part
fixes the net sidewise difference of trivial-zero capacity, but it does not
determine the individual alpha split across atoms.

The packet-side finite-part requirement is recorded in
`docs/net_finite_part_packet_sum_requirement.md`: the net completion balance
requires a regularized global first-moment identity over all deconvolved
prime-power packets, not an ordinary partial-sum limit from local GWR
envelopes.

The existing-machinery survey is recorded in
`docs/existing_global_machinery_survey_for_packet_balance.md`: current PGS
theorems, zero-excess coordinate work, grammar/motif surfaces, and
bounded-compression records can frame the packet finite-part target, but they
do not prove the required global chamber-centered summation law.

The candidate global principle is outlined in
`docs/chamber_centered_von_mangoldt_finite_part_strategy.md`: subtract the
endpoint linear drift from the chamber-centered von Mangoldt first moment and
prove that the remaining finite part equals the centered trivial-zero finite
part.

The endpoint drift summatory candidate is recorded in
`docs/endpoint_log_gap_summatory_law_candidate.md`: the endpoint log-gap sum
has candidate leading term `0.5 log(X)^2`, reducible to a reciprocal endpoint
occupancy theorem plus nonlinear log-gap error control.

The reciprocal endpoint occupancy candidate is recorded in
`docs/reciprocal_endpoint_occupancy_theorem_candidate.md`: the right-endpoint
samples `g(q) log(q)/q` have the harmonic finite part if the reciprocal
square-gap energy is finite, or if the sampling error is proved finite
directly.

The reciprocal gap-energy obstruction is recorded in
`docs/reciprocal_gap_energy_bound_obstruction.md`: endpoint occupancy reduces
to a global dyadic second-moment theorem for prime-gap widths, which current
local GWR machinery does not prove.

The reciprocal gap-energy proof strategy is recorded in
`docs/reciprocal_gap_energy_proof_strategy.md`: the dyadic second-moment bound
is reduced to a zero-excess return-time tail theorem for long chambers.

Candidate routes toward that return-time theorem are recorded in
`docs/zero_excess_return_time_tail_strategy.md`: a divisor-field recurrence
estimate or a quantitative chamber grammar tail law would be sufficient, but
neither is currently proved by local GWR machinery.

The divisor-field recurrence route is sharpened in
`docs/positive_excess_persistence_estimate_candidate.md`: first-moment
occupancy and excess area give only an `H^-1` tail, so the missing estimate is
a quadratic persistence cost for long positive-excess runs.

The direct second-moment route is stated in
`docs/persistence_energy_inequality_strategy.md`: reciprocal gap energy follows
from a dyadic bound on accumulated zero-excess age, or from a stronger
age-weighted excess energy bound.

The candidate recurrence potential is recorded in
`docs/age_divisor_recurrence_potential_candidate.md`: the PGS-native potential
`(n-p(n))(\tau(n)-2)` gives quadratic per-run cost, but its dyadic total bound
is a new global theorem obligation.

The GWR-to-age transfer obstruction is recorded in
`docs/gwr_to_age_transfer_obstruction.md`: GWR controls a local chamber
minimum, while the age-divisor theorem needs an upper bound on accumulated
whole-chamber persistence energy.

The chamber upper-invariant candidate is recorded in
`docs/chamber_age_energy_upper_invariant_candidate.md`: a divisor-channel age
budget bounds chamber age-divisor energy, reducing the remaining proof to
dyadic divisor-channel age orthogonality.

The divisor-channel age orthogonality route is recorded in
`docs/divisor_channel_age_orthogonality_strategy.md`: it reduces the dyadic
summation of the upper invariant to uniform endpoint-modulus age recurrence
over divisor channels `d <= sqrt(2X)`.

The endpoint-modulus recurrence target is recorded in
`docs/endpoint_modulus_age_recurrence_strategy.md`: for each divisor channel
`d`, multiples of `d` must have average backward distance to the previous
zero-excess endpoint bounded by a polylogarithm.

The modulus-link route is recorded in
`docs/modulus_link_endpoint_lattice_closure_strategy.md`: Endpoint-Modulus Age
Recurrence would follow from a uniform endpoint-lattice crossing-energy bound
for every divisor channel `d <= sqrt(2X)`.

The endpoint-lattice invariant obstruction is recorded in
`docs/endpoint_lattice_closure_invariant_obstruction.md`: the `d = 2` channel
already contains one quarter of the dyadic gap-square sum, so modulus-link
closure cannot bypass the global zero-excess return-time obstruction.

The endpoint-chain reductions are consolidated in
`docs/zero_excess_return_square_moment_consolidation.md`: persistence-energy,
endpoint occupancy, and modulus-link formulations all require the same dyadic
square-moment theorem for zero-excess return times.

The square-moment attack surface is consolidated in
`docs/zero_excess_square_moment_attack_consolidation.md`: the age-divisor
recurrence potential is the strongest current PGS-native line, while every
route still needs a new global return-time frequency theorem.

The focused even-channel subproblem is recorded in
`docs/even_channel_age_recurrence_attack.md`: bounding the average backward
distance from even integers to the previous zero-excess endpoint is equivalent
to the dyadic square-moment theorem.

The odd-offset persistence step is recorded in
`docs/odd_offset_persistence_estimate_strategy.md`: long even-channel age is
equivalent to complete positive-excess coverage of many odd offsets before an
even center, reducing the problem to an odd divisor-covering tail theorem.

The covering-tail attack is recorded in
`docs/odd_divisor_covering_tail_lcm_strategy.md`: a long odd-offset cover
should force a canonical least-divisor congruence certificate with large LCM,
making persistent even centers rare.

The LCM-growth subproblem is reduced in
`docs/odd_cover_lcm_growth_jacobsthal_reduction.md`: a complete odd-offset
proper-divisor cover gives a shifted Jacobsthal gap for the radical of its
certificate LCM, so the standard Jacobsthal window bound closes the LCM-growth
part while leaving certificate entropy as the next counting obstruction.

The entropy obstruction is stated in
`docs/canonical_least_divisor_word_entropy_obstruction.md`: fixed canonical
least-divisor words have density `X/L`, but the full odd-covering tail needs a
weighted shifted-sieve bound on the total measure of realized words so the
union over certificates does not erase the LCM saving.

Candidate proof routes for that entropy theorem are recorded in
`docs/canonical_word_entropy_proof_routes.md`: canonical words are rewritten
as essential first-hit residue covering systems, reducing the next obligation
to an essential shifted-cover weight estimate for least-divisor channels.

The first proof route for that weight estimate is recorded in
`docs/uncovered_set_decay_for_essential_cover_weight.md`: least-divisor
exclusions are built into an uncovered-set process over increasing odd prime
channels, reducing the entropy problem to a shifted-sieve extinction estimate.

The analytic extinction attack is recorded in
`docs/fourth_moment_uncovered_set_extinction_strategy.md`: a fourth central
moment bound for the uncovered survivor count would turn extinction into an
inverse-square tail, while a second-moment bound gives only the already-known
inverse-first-power obstruction.

The fixed-tuple sieve route is recorded in
`docs/four_tuple_selberg_bound_strategy.md`: the four-tuple moment input
splits into a dimension-at-most-four Selberg upper bound for each shifted odd
linear-form tuple plus an average singular-series estimate over offset
collisions.

The singular-series averaging route is recorded in
`docs/four_offset_singular_series_average_strategy.md`: for at most four
offsets, singular factors are bounded by pair-difference collision products,
reducing the average to a finite divisor-correlation estimate.

The divisor-correlation estimate is recorded in
`docs/graph_rank_divisor_correlation_estimate.md`: after Möbius expansion,
each prime contributes according to the rank of its edge congruence graph, and
every nonempty prime-edge pattern pays at least a `p^-2` local cost.

The proof skeleton is recorded in
`docs/graph_rank_divisor_correlation_proof_skeleton.md`: diagonal classes are
removed, the pair factors are expanded, the congruence lattice index factors
by prime graph rank, and the finite-box boundary term is absorbed by a
polylogarithmic divisor sum.

The first-moment input is recorded in
`docs/first_moment_endpoint_density_for_shifted_offsets.md`: for offsets
`2s+1 <= X`, the mean survivor count reduces to prime endpoint density in the
shifted intervals `[X-(2s+1), 2X-(2s+1)]`, with parity automatic and the
long-offset boundary isolated.

The long-offset boundary is closed in
`docs/long_offset_boundary_bertrand_closure.md`: Bertrand's postulate gives
`p(M)>M/2`, so centers `M in [X,2X]` have age `< X` and cannot support a
complete odd-offset cover once `N > X/2+O(1)`.

The uncovered-set assembly state is recorded in
`docs/fourth_moment_extinction_assembly_status.md`: endpoint-density mean,
Bertrand boundary closure, and graph-rank singular averaging are integrated,
leaving the uniform fixed-tuple Selberg upper bound and fourth-central-moment
assembly as the precise remaining inputs for the `H^-2` tail.

The fixed-tuple Selberg strategy is recorded in
`docs/uniform_four_form_selberg_remainder_strategy.md`: use a finite
Selberg level `D=X^(1/4)` for the shifted odd forms, obtain the singular-factor
main term from the sieve denominator, and absorb the accumulated dyadic
remainder after summing over tuples of size at most four.

The fourth-moment assembly obstruction is recorded in
`docs/fourth_central_moment_assembly_obstruction.md`: raw Selberg upper
bounds do not by themselves give a centered fourth moment; the remaining input
is a factorial-moment asymptotic or direct concentration lemma for endpoint
survivors.

Candidate concentration routes are recorded in
`docs/fourth_moment_endpoint_concentration_routes.md`: the survivor count is
identified with the number of prime endpoints in `(M-2N,M)`, reducing the
remaining input to a fourth moment for moving short-interval endpoint counts.

The direct concentration strategy is recorded in
`docs/direct_short_interval_endpoint_fourth_moment_strategy.md`: expand the
moving endpoint count as overlaps of prime-generated intervals and reduce the
remaining input to a centered four-point endpoint energy, or discrete Selberg
fourth-integral, estimate at scale `N`.

The centered-energy target is recorded in
`docs/centered_four_point_endpoint_energy_strategy.md`: after subtracting the
endpoint density, the fourth moment becomes a centered four-endpoint overlap
energy, equivalently an `L^2` bound for the additive convolution of the
kernel-smoothed endpoint Fourier transform.

The Fourier attack is recorded in
`docs/major_minor_arc_endpoint_energy_strategy.md`: decompose the
kernel-smoothed centered endpoint transform into major arcs carrying
residue-class singular structure and minor arcs requiring strong endpoint
exponential-sum dispersion.

The minor-arc target is recorded in
`docs/minor_arc_endpoint_exponential_sum_strategy.md`: the needed estimate is
a kernel-weighted, convolution-strength bound for centered endpoint
exponential sums, with Vaughan-type, weighted large-sieve, and zero-density
routes identified.

The bilinear minor-arc strategy is recorded in
`docs/minor_arc_bilinear_kernel_bound_strategy.md`: decompose the interval
kernel into dyadic frequency bands and seek a weighted large-sieve plus
Type I/II bilinear estimate strong enough after additive convolution.

The Type II bottleneck is recorded in
`docs/type_ii_bilinear_endpoint_kernel_strategy.md`: after Vaughan or
Heath-Brown decomposition, balanced bilinear endpoint sums must satisfy a
kernel-band frequency-pair dispersion estimate strong enough for the
minor-arc convolution norm.

The minor-major resonance case is recorded in
`docs/type_ii_minor_major_resonance_strategy.md`: when a minor Type II
frequency is paired with a major-arc frequency in the convolution, the proof
needs shifted Type II estimates uniform in small moduli plus zero-density or
Bombieri-Vinogradov control of major-arc transition errors.

The shifted Type II target is recorded in
`docs/shifted_type_ii_small_modulus_strategy.md`: small rational major-arc
shifts `a/q` reduce to Type II sums in residue classes modulo `q`, requiring a
hybrid large-sieve or character-sum estimate in the balanced range after
kernel weighting.

The hybrid large-sieve core is recorded in
`docs/hybrid_large_sieve_shifted_type_ii_strategy.md`: after Cauchy, shifted
Type II estimates reduce to spacing and near-product-difference bounds for
frequencies `m(theta+a/q)` with small-modulus congruence splitting.

The near-product dispersion target is recorded in
`docs/near_equal_product_dispersion_strategy.md`: expanding the shifted Type
II mean square leaves kernel-weighted off-diagonal sums over
`mn-m'n'`, requiring multiplicative dispersion with small-modulus oscillation
after major-arc coherent parts are removed.

The dispersion-method reduction is recorded in
`docs/dispersion_method_near_product_sum_strategy.md`: common-divisor
splitting rewrites the product difference as `d(un-vn')`, additive Fourier
expansion exposes paired slope frequencies, and the remaining target is a
projected bilinear dispersion estimate after exact diagonals and
small-conductor coherent pieces are removed.

The projected linear cancellation target is recorded in
`docs/projected_linear_congruence_cancellation_strategy.md`: after the major
projector removes diagonals, residue-class means, and low-denominator kernel
packets, the required input is Poisson-scale average cancellation for the
coprime-slope family `un-vn'=t`, supplied by some combination of hybrid
large-sieve spacing, linear dispersion, and bilinear Kloosterman estimates.

The bilinear discrepancy input ledger is recorded in
`docs/bilinear_discrepancy_inputs_for_projected_slope_congruences.md`: the
local graph sum `n'=(un-t)/v` must cancel on average over coprime balanced
slopes after the major projector removes the components that large-sieve,
dispersion, and Kloosterman estimates cannot cancel.

The reciprocal Kloosterman branch is recorded in
`docs/bilinear_kloosterman_average_strategy.md`: completing the congruence
`n' = -t vbar mod u` turns the slope average into weighted sums
`S(-ht,k;u)`, so closure requires a projected bilinear Kloosterman or spectral
large-sieve estimate after zero-frequency and low-conductor pieces are
removed.

The spectral Kloosterman strategy is recorded in
`docs/kuznetsov_spectral_large_sieve_kloosterman_strategy.md`: collapse the
completed product argument `m=-ht`, put the modulus average into Kuznetsov
shape, and control the resulting spectral sums by large-sieve bounds for the
collapsed `C_m` and completed slope `D_k` coefficient sequences.

The collapsed spectral size ledger is recorded in
`docs/collapsed_coefficient_spectral_size_control.md`: Cauchy bounds the
collapse `C_m=sum_{ht=-m}A_{h,t}` by divisor powers, Plancherel controls the
completed slope sequence `D_k`, and closure requires the Bessel-selected
spectral range to keep the large-sieve cost inside the Poisson allowance.

The collapsed size closure criteria are recorded in
`docs/collapsed_coefficient_size_closure_criteria.md`: for each dyadic
`d`-slice and kernel band, Cauchy plus Plancherel close the spectral branch
exactly when the completion energies `E_A(d,N)`, `E_v(d)`, and the
Bessel-selected range `R_spec` satisfy the explicit Poisson-allowance
inequality.

The kernel-band completion-energy verification status is recorded in
`docs/kernel_band_completion_energy_verification_status.md`: the current
notes fix dyadic band weights and Poisson budgets, but they do not yet define
the transform norms and support of `W_{N,L}` tightly enough to verify the
Completion Energy Bound, so the next minimal input is a kernel-band
completion-energy lemma after major-exclusion.

The kernel-band completion-energy proof strategy is recorded in
`docs/kernel_band_completion_energy_proof_strategy.md`: smoothing a dyadic
band gives `||w_{N,L}||_2^2 << L^3`, Parseval and sampling at `r=dt` give
`sum_t |W_{N,L}(dt)|^2 << d^{-1}L^3`, and the base completion-energy bound is
`E_A(d,N,L) << d^{-1}L^3 B_d` unless projected band mean-value saving is
needed.

The projected kernel-band saving strategy is recorded in
`docs/projected_kernel_band_saving_strategy.md`: improvement over the base
`L^3` energy must come either from the major projector capturing most high-`L`
band energy, from orthogonality to low-denominator packet functions, or from
cross-band/rational-shift cancellation before absolute squaring.

The cross-band transform strategy is recorded in
`docs/cross_band_transform_overlap_strategy.md`: Poisson summation converts
sampled cross terms between `W_{N,L}^{perp}(dt)` and
`W_{N,L'}^{perp}(dt)` into shifted overlaps of projected band weights, with
nonresonant overlaps controlled by a shifted-band lemma and resonant diagonal
energy left to the projected mean-value estimate.

The nonresonant shifted-band overlap strategy is recorded in
`docs/nonresonant_shifted_band_overlap_strategy.md`: projected dyadic kernel
bands are modeled as annuli of width `L^{-1}` around low-conductor packet
centers, so shifted overlaps decay rapidly unless the shift aligns a rational
center difference within the combined band width.

The major-aperture removal strategy is recorded in
`docs/major_aperture_kernel_band_removal_strategy.md`: diagonal high-`L`
kernel energy is removed only when the major aperture radius around a
low-conductor packet center is coupled to the dyadic band scale, or when an
orthogonal major-packet projector captures the same band energy.

The major-aperture radius selection strategy is recorded in
`docs/major_aperture_radius_selection_strategy.md`: literal aperture removal
closes high-`L` diagonal energy exactly when the radius interval
`c0/L_crit(d,N) <= rho_c <= min(c1 Q0^{-2}, rho_valid(c))` is nonempty; if it
is empty, an orthogonal major-packet frame estimate must replace support
removal.

The major-aperture feasibility verification strategy is recorded in
`docs/major_aperture_feasibility_verification_strategy.md`: verification
requires computing `L_crit(d,N)`, proving a major validity radius
`rho_valid(c)`, and comparing both against rational separation, or replacing
literal support removal by a lower frame bound for major packet functions.

The major validity radius strategy is recorded in
`docs/major_validity_radius_strategy.md`: `rho_valid(a/q)` is defined by a
kernel-weighted error budget for the centered endpoint major approximation on
`alpha=a/q+beta`, with PNT-in-AP, Bombieri-Vinogradov, and zero-density inputs
supplying the required radius/error tradeoff.

The BV/zero-density radius tradeoff is recorded in
`docs/bv_zero_density_major_radius_tradeoff.md`: `rho_valid(a/q)` is the
largest radius for which `(E_q(X)^2/log^2 X) I_N(a/q,rho)` fits the assigned
packet budget, so BV or zero-density input must make this true at
`rho=c0/L_crit(d,N)`.

The budget-weighted BV radius strategy is recorded in
`docs/budget_weighted_bv_radius_strategy.md`: the averaged aperture route is
a major-window mean-square estimate for `Err_{a/q}(beta)` weighted by
`|K_N(a/q+beta)|^2`, supplied by a square-mean AP theorem or continuous large
sieve strong enough at `rho=c0/L_crit(d,N)`.

The BDH strength versus kernel cost closure is recorded in
`docs/bdh_strength_kernel_cost_closure.md`: the averaged radius route closes
when the square-mean AP error, preferably in kernel-weighted form, satisfies
`B_AP^K(X,Q0,N,rho)/log^2 X <= E_tot(d,N)` at
`rho=c0/L_crit(d,N)`.

The closest-known-form BDH strategy is recorded in
`docs/kernel_weighted_bdh_known_form_strategy.md`: standard BDH supplies the
right square-mean AP object and the large sieve supplies continuous-frequency
control, but the bridge needs their kernel-weighted major-window form after
major-packet subtraction.

The kernel-weighted maximal BDH proof strategy is recorded in
`docs/kernel_weighted_maximal_bdh_proof_strategy.md`: after character
decomposition and major-packet subtraction, the new feature beyond standard
BDH is the `a`-dependent weight `|K_N(a/q+beta)|^2`, requiring a hybrid
character/additive large sieve over the windows `a/q+beta`.

The hybrid character-additive large-sieve strategy is recorded in
`docs/hybrid_character_additive_large_sieve_strategy.md`: the required
weighted theorem estimates residual character sums over continuous
`beta`-windows, while the dual expansion is a shifted congruence correlation
`n == m mod q` with `|n-m|` controlled by the aperture width.

The dual shifted-congruence strategy is recorded in
`docs/dual_shifted_congruence_large_sieve_strategy.md`: expanding the hybrid
large sieve turns `n == m mod q` into shifts `h=n-m` with `q|h` and
`|h| <= rho^{-1}`, so either the large-sieve operator norm fits the budget or
one needs a divisor-weighted shifted-prime correlation estimate after
major-packet subtraction.

The divisor-weighted shifted-correlation strategy is recorded in
`docs/divisor_weighted_shifted_correlation_strategy.md`: the hard branch is
an averaged shifted-prime correlation with weight `tau_{Q0}(h)`, equivalently
a Fourier mean value of `|A(alpha)|^2` against a divisor-weighted kernel, and
it loops back to Type I/II dispersion if large-sieve positivity is too weak.

The weighted minor-arc shift-kernel mean-value strategy is recorded in
`docs/weighted_minor_arc_shift_kernel_mean_value_strategy.md`: the multiplier
`F_{Q0,H,N}` is a truncated divisor exponential sum with low-denominator
peaks assigned to major packets, leaving a residual minor-arc estimate for
`|A^perp(alpha)|^2 F_min(alpha)`.

The divisor shift-kernel minor-arc bound strategy is recorded in
`docs/divisor_shift_kernel_minor_arc_bound_strategy.md`: decomposing
`tau_{Q0}(h)` by divisors turns `F` into smoothed geometric sums
`sum_r Omegahat(qr)e(qr alpha)`, so after removing small `||q alpha||` peaks,
the residual is bounded by geometric-sum or divisor-square mean-value
estimates.

The shift-kernel minor-threshold strategy is recorded in
`docs/shift_kernel_minor_threshold_selection.md`: thresholds `eta_q` must be
large enough to make `sum_q V_q min(H/q,1/eta_q)` fit the shifted-error
budget, while `eta_q/q` remains inside valid and disjoint major apertures; if
this fails, the route switches to an `L2` divisor-kernel estimate plus an
endpoint `L4` moment.

The shift-kernel threshold verification strategy is recorded in
`docs/shift_kernel_threshold_feasibility_verification.md`: choosing the
maximal admissible threshold `eta_q=q rho_max(q)` reduces feasibility to one
inequality involving `V_q`, `rho_valid`, `A_min`, and `E_shift`; the `L2`
alternative is explicitly noncircular only with an independent residual
endpoint `L4` moment.

The kernel transform variation bound is recorded in
`docs/kernel_transform_variation_bound_strategy.md`: for a smoothed projected
aperture weight `Omega_N` supported on `|beta|<=rho`, the samples
`Omegahat_N(qr)` have size and discrete variation `V_q << ||Omega_N||_1`
uniformly for `q<=Q0`.

The post-variation threshold closure is recorded in
`docs/shift_kernel_threshold_closure_after_variation.md`: substituting
`V_q << ||Omega_N||_1` reduces the `L^infty` shift-kernel route to the scalar
condition `||Omega_N||_1 S(Q0,H,rho_max) A_min <= E_shift`.

The kernel-window mass bound is recorded in
`docs/kernel_window_mass_bound_strategy.md`: `||Omega_N||_1` is controlled by
support geometry relative to the interval-kernel peak, with major-aperture
core removal reducing peak-scale mass to an annular tail bound, or by an
orthogonal projection-energy estimate.

The aperture-core removal mass strategy is recorded in
`docs/aperture_core_removal_mass_strategy.md`: the mass target
`M_Omega=E_shift/(S A_min)` is met by literal support removal when the major
projector excludes the kernel peak core to radius `rho_core >= 1/M_Omega`
inside the valid and disjoint major aperture.

The major projector core-radius verification is recorded in
`docs/major_projector_core_radius_verification.md`: for each relevant kernel
peak center, the major projector must remove or capture the core of radius
`M_Omega^{-1}` while staying inside `rho_valid(c)` and rational separation,
or prove the same residual mass bound by packet-frame projection.

The unified major aperture radius verification is recorded in
`docs/unified_major_aperture_radius_verification.md`: literal support removal
requires the single combined radius
`max(c0/L_crit(d,N), M_Omega^{-1})` to fit inside both `rho_valid(c)` and
`c1 Q0^{-2}`, otherwise a packet-frame projection must capture both band
energy and kernel-window mass.

The unified major validity radius proof strategy is recorded in
`docs/unified_major_validity_radius_proof_strategy.md`: the major
approximation must hold at
`R_req=max(c0/L_crit(d,N), M_Omega^{-1})`, with kernel-weighted error bounded
pointwise by PNT-in-AP/zero-density or on average by a kernel-weighted BDH
input, while preserving rational separation.

The kernel-weighted BDH theorem at unified scale is recorded in
`docs/kernel_weighted_bdh_at_unified_scale.md`: the averaged major-validity
route requires major-window square-mean control at
`R_req=max(c0/L_crit, M_Omega^{-1})`, strong enough to pay for both high-band
aperture removal and shift-kernel core-mass control.

The hybrid large-sieve closure at unified scale is recorded in
`docs/hybrid_large_sieve_unified_scale_closure.md`: the direct averaged route
closes when the weighted hybrid large-sieve constant at `R_req` times the
residual coefficient energy `A_2` is at most `log^2 X E_maj`; otherwise the
proof must use the dual shifted-congruence route.

The primal weighted hybrid large-sieve strategy is recorded in
`docs/primal_weighted_hybrid_large_sieve_strategy.md`: prove an operator-norm
estimate for the actual kernel-weighted frequency measure on
`a/q+beta`, with constant controlled by weighted multiplicative and additive
concentration rather than by charging every packet the worst kernel peak.

The direct continuous-frequency large-sieve strategy is recorded in
`docs/direct_continuous_frequency_large_sieve_strategy.md`: bypass the
`a`-weighted character orthogonality issue by estimating the residual endpoint
exponential sum directly over the weighted measure on `alpha=a/q+beta`, with
constant controlled by total mass and local `1/X` concentration.

The kernel-weighted measure concentration bounds are recorded in
`docs/kernel_weighted_measure_concentration_bounds.md`: bounded rational-window
overlap gives `mu([0,1]) << N` and local concentration `<< N^2/X`, while
aperture-core removal to radius `rho_core` improves the direct large-sieve
constant to `O(rho_core^{-2}+rho_core^{-1})`.

The direct large-sieve scalar closure is recorded in
`docs/direct_large_sieve_scalar_closure.md`: after concentration bounds, the
direct additive route closes when the major projector removes the kernel core
to radius at least `R_LS`, producing the unified aperture demand
`R_all=max(c0/L_crit, M_Omega^{-1}, R_LS) <= min(rho_valid,c1Q0^{-2})`.

The full unified major aperture verification strategy is recorded in
`docs/full_unified_major_aperture_verification_strategy.md`: the literal
support route closes exactly when `R_all` fits inside both major validity and
rational separation for every relevant center; if not, a packet-frame theorem
must directly supply band-energy, kernel-mass, and measure-concentration
residual bounds.

The literal support-removal verification is recorded in
`docs/literal_radius_support_removal_verification.md`: support removal closes
the full aperture lemma when `R_all` satisfies rational separation and the
centered endpoint major approximation has kernel-weighted error inside its
budget on `|beta|<=R_all`, pointwise or via unified-scale kernel-weighted BDH.

The full-radius major validity obligation is recorded in
`docs/full_radius_major_validity_obligation.md`: the literal support-removal
route now depends on proving kernel-weighted major validity at
`R_all=max(c0/L_crit,M_Omega^{-1},R_LS)` by pointwise AP/zero-density,
kernel-weighted BDH, or direct continuous-frequency large sieve.

The concrete full-radius BDH proof plan is recorded in
`docs/full_radius_bdh_direct_proof_plan.md`: choose the direct
continuous-frequency large-sieve route; because `R_all` includes `R_LS`, the
large-sieve operator budget closes once residual coefficient energy, budget
assignment, major validity radius, and rational separation are verified.

The noncircular direct major-validity clarification is recorded in
`docs/noncircular_major_validity_direct_route.md`: in the direct averaged
route, `rho_valid >= R_all` is proved by the continuous-frequency large sieve
after explicit major packet subtraction, not assumed from the same
kernel-weighted BDH theorem.

The major packet model subtraction strategy is recorded in
`docs/major_packet_model_subtraction_strategy.md`: the direct additive
large-sieve route requires a coefficient-level residual sequence produced by
a bounded projection onto low-conductor major packet components; if only local
Fourier packet subtraction is available, the proof must use the weighted
hybrid character large-sieve route instead.

The low-conductor major projection strategy is recorded in
`docs/low_conductor_major_projection_strategy.md`: build `V_maj` from additive
packet vectors `e(-cn)` for low-conductor centers, define `P_maj` by a Gram
projection, and use rational spacing plus frame bounds to reproduce local
major packet models on `|beta|<=R_all`.

The low-conductor frame conditioning strategy is recorded in
`docs/low_conductor_frame_conditioning_strategy.md`: additive large-sieve and
Gram/Riesz bounds control the packet frame and give `A_2<=||a||_2^2` for the
orthogonal residual, while matching classical major amplitudes remains a
separate AP input.

The major projection amplitude reproduction strategy is recorded in
`docs/major_projection_amplitude_reproduction_strategy.md`: projection
coefficients are packet correlations of the endpoint weight; PNT in AP,
partial summation, and Gram conditioning identify them with the classical
`mu(q)/phi(q)` major amplitudes up to the assigned major error budget.

The direct full-radius BDH assembly is recorded in
`docs/direct_full_radius_bdh_assembly.md`: coefficient-level major projection,
amplitude reproduction, residual energy, measure concentration, and the
unified radius condition combine to prove the direct kernel-weighted BDH
estimate at `R_all`; the remaining gaps are quantitative frame, AP-error,
budget-consistency, and radius-feasibility checks.

The frame and amplitude gap closure strategy is recorded in
`docs/frame_and_amplitude_gap_closure_strategy.md`: for polylogarithmic
`Q0`, the low-conductor frame is conditioned by `Q0^4<<X` and amplitude
reproduction follows from Siegel-Walfisz/PNT-in-AP, while larger `Q0` needs
sharper frame and averaged or zero-density AP inputs.

The budget consistency and radius feasibility strategy is recorded in
`docs/budget_consistency_radius_feasibility_strategy.md`: assign `Q0`,
external major-validity radii, `E_maj`, `E_shift`, `A_2`, and `A_min` before
computing `R_all`, then verify `R_all <= min(rho_valid^0,c1Q0^{-2})` without
using the direct BDH conclusion circularly.

The direct route literal support closure plan is recorded in
`docs/direct_route_literal_support_closure_plan.md`: the direct full-radius
BDH route is now a finite checklist of budget/radius feasibility,
frame-amplitude closure, residual energy, and measure concentration; if the
literal radius condition fails, the packet-frame alternative must directly
capture band energy, kernel mass, and measure concentration.

The polylogarithmic verification plan is recorded in
`docs/polylog_budget_frame_verification_plan.md`: for
`Q0=(log X)^B`, frame conditioning and amplitude reproduction close by
diagonal dominance and Siegel-Walfisz, leaving four explicit scalar
budget/radius inequalities for the literal route; if any fail, the unified
packet-frame lower bound must directly control residual band energy, kernel
mass, and weighted measure concentration.

The scalar inequality verification strategy is recorded in
`docs/scalar_budget_inequality_verification_strategy.md`: in the clean
literal branch `Q0 <= (log X)^(1/2-epsilon)`, Siegel-Walfisz supplies the
major radius and the remaining obligations are three completion-side lower
bounds for `T_{d,N}`, `E_shift`, and `E_maj/A_2`; beyond that budget range,
the unified packet-frame lower bound is the only noncircular replacement.

The PGS completion budget source ledger is recorded in
`docs/pgs_completion_budget_source_ledger.md`: it identifies the exact
completion quantities that must supply the three lower bounds, namely the
projected kernel-band transform allowance `T_{d,N}`, the shifted-kernel ratio
`E_shift/A_min`, and the major-window ratio `E_maj/A_2`, with the unified
packet-frame theorem as the direct residual-capture replacement.

The kernel-band budget verification strategy is recorded in
`docs/kernel_band_budget_verification_strategy.md`: the first completion-side
source now has one test, the allowance ratio
`Xi_{d,N} >= C Q0^6`; if that fails, the exact replacement is a packet-frame
band-energy lower bound giving `Delta_L <= Xi_{d,N}/L^3`.

The shift-kernel budget ratio strategy is recorded in
`docs/shift_kernel_budget_ratio_strategy.md`: literal core removal at radius
`rho_core ~ Q0^(-2)` gives `||Omega_N^perp||_1 << Q0^2`, reducing the
shift-kernel branch to the scalar test
`E_shift/A_min >= C Q0^2 min(H,Q0^2) log Q0`; if support removal fails, the
packet-frame replacement must prove the same residual `L1` mass bound
directly.

The major-window budget ratio strategy is recorded in
`docs/major_window_budget_ratio_strategy.md`: after coefficient-level major
projection, literal core removal gives direct large-sieve operator constant
`L_mu << Q0^4`, reducing the major-window branch to
`E_maj/A_2 >= C Q0^4/log^2 X`; if support removal fails, the packet-frame
replacement must prove the same concentration bound for the projected
residual measure `mu^perp`.

The completion allowance budget split strategy is recorded in
`docs/completion_allowance_budget_split_strategy.md`: the literal direct
route closes once the total completion allowance pays the band, shift,
major-window, and tail requirements simultaneously; if the split fails, the
Unified Packet-Frame Source theorem must lower those payments by direct
residual capture.

The final completion verification matrix is recorded in
`docs/final_completion_verification_matrix.md`: `PROOF.md` supplies the local
PGS source theorems, while the downstream direct full-radius BDH route now
depends on a finite matrix of completion quantities against `E_tot`, or on
one Unified Packet-Frame Source theorem that proves the three residual
captures simultaneously.

The kernel-band allowance total verification is recorded in
`docs/kernel_band_allowance_total_verification.md`: the band payment is now a
single dyadic-sum estimate
`sum d^(-1) B_d D_{d,N} <= E_band^alloc/(C Q0^6 log^C X)`, with the
packet-frame replacement given by the same sum after inserting projected
saving factors `Delta_L`.

The kernel-band dyadic load parameter strategy is recorded in
`docs/kernel_band_dyadic_load_parameter_strategy.md`: after substituting the
Bessel range, the band source is reduced to an exponent-and-log check in the
dyadic sizes `d,U,H_d,T_d,K_d,B_d,E_v(d)`, with packet-frame saving
`Delta_L` as the exact replacement when the load is too large.

The kernel-band support, norm, and transform input ledger is recorded in
`docs/kernel_band_support_norm_transform_inputs.md`: `D_{d,N}` is now
expanded into physical lengths, Poisson dual supports, transform support
`T_d << L/d`, divisor-norm laws for `B_d` and `E_v(d)`, and the Bessel range;
the dual supports include the central-frequency terms
`H_d << 1+U/N_d` and `K_d << 1+U/V_d`.

The kernel-band physical-length closure test is recorded in
`docs/kernel_band_physical_length_closure_test.md`: substituting
`H_d << 1+U/N_d`, `K_d << 1+U/V_d`, `T_d << L/d`, `B_d << N_d`, and
`E_v(d) << V_d` turns the band payment into a physical-length sum controlled
by the Bessel factor, `L/d` support factor, and slope completion factor, with
`Delta_L` as the exact packet-frame saving if the sum is too large.

The Type II physical length assignment is recorded in
`docs/type_ii_physical_length_assignment.md`: the common-divisor split gives
`U ~ A/d`, `N_d ~ B`, and `V_d ~ A/d`, exposing the balanced-range high-band
pressure term `L/d`; if that term exceeds the band allocation, the remaining
remedy is packet-frame `Delta_L` saving or a sharper Type II coefficient norm.

The Type II summed physical load test is recorded in
`docs/type_ii_summed_physical_load_test.md`: summing the balanced diagnostic
over paid bands `L <= Q0^2` gives the explicit literal band requirement
`E_band,bal^req << X^(3/2) Q0^8 log^C X`; if the allocated band budget is
smaller, the needed packet-frame saving is the corresponding average or
slice-level `Delta_L`.

The Type II unbalanced range dominance note is recorded in
`docs/type_ii_unbalanced_range_dominance.md`: with `A <= B`, a fixed Type II
floor `A >= X^theta`, and polylogarithmic `Q0`, the unbalanced ranges are no
worse than the balanced range, so the full Type II literal band payment is
`E_band,TypeII^req << X^(3/2) Q0^8 log^C X`.

The Type II band budget versus Poisson allowance comparison is recorded in
`docs/type_ii_band_budget_poisson_comparison.md`: comparing the explicit band
payment with the convolution-scale allowance `X(N/log X)^2` shows that the
literal Type II band budget closes for
`N >= X^(1/4) Q0^4 log^C X`; below that threshold the packet-frame theorem
must supply average saving
`Delta_bar << N^2/(X^(1/2) Q0^8 log^C X)`.

The Direct Full-Radius BDH polylog literal closure is recorded in
`docs/direct_full_radius_bdh_polylog_literal_closure.md`: for
`Q0=(log X)^B` with `B <= 1/2-epsilon`, residual energy envelopes
`A_2,A_min <= X log^C X`, Siegel-Walfisz major validity, and
`N >= X^(1/4) Q0^4 log^C X`, all four scalar gates fit inside the completion
allowance; outside this regime, the Unified Packet-Frame Source theorem is
the remaining direct-route obligation.

The polylog literal closure input verification is recorded in
`docs/polylog_literal_closure_input_verification.md`: the residual energy
envelopes follow from projection boundedness and Parseval, major validity
comes from Siegel-Walfisz for polylogarithmic conductors, the Poisson
allowance is a normalization contract, and the remaining literal-route bridge
parameter is the kernel length condition
`N >= X^(1/4) Q0^4 log^C X`.

The polylog completion parameter assignment is recorded in
`docs/polylog_completion_parameter_assignment.md`: choosing
`Q0=(log X)^(1/4)` and
`N=X^(1/4)(log X)^(1+C_N)` pays the Type II band, shift-kernel,
major-window, and tail budgets inside the Poisson allowance, provided the RH
bridge permits that interval-kernel length; otherwise the packet-frame
`Delta_L` theorem is required.

The Direct Full-Radius BDH closure certificate is recorded in
`docs/direct_full_radius_bdh_closure_certificate.md`: it certifies the
explicit polylog literal assignment gate by gate and records the consequence
chain through Full-Radius Major Validity and Full Unified Major Aperture; the
only direct-route boundary left in the certificate is kernel-length
admissibility.

The Direct BDH to completion transport interface is recorded in
`docs/direct_bdh_to_completion_transport_interface.md`: the closed direct BDH
route supplies major residual control, kernel-band completion-energy control,
and controlled summation for the completion assembly, while the remaining
bridge input is packetwise completion localization giving
`rho_{p,q}(z) >= M_{p,q}`.

The packetwise completion localization strategy is recorded in
`docs/packetwise_completion_localization_strategy.md`: localization follows
from `M_{p,q}<1/2`, pole/trivial-zero transport radii at least `1/2`, and
excluding zero-radius constants from negative folded cost; the remaining
completion work is transport capacity balance plus exact assembly.

The transport capacity balance and exact assembly strategy is recorded in
`docs/transport_capacity_balance_assembly_strategy.md`: with Direct BDH
supplying controlled summation, capacity balance reduces to the finite-part
packet identity plus a nonnegative common side-deficit check; if both hold, a
deterministic symmetric trivial-zero split supplies the missing sidewise
capacity while preserving exact assembly atom by atom.

The Chamber-Deconvolved Balance reduction assembly is recorded in
`docs/chamber_deconvolved_balance_reduction_assembly.md`: Direct BDH,
packetwise localization, and symmetric transport capacity balance reduce the
Chamber-Deconvolved Reciprocal Balance Lemma to the Folded Packet Drift
Inequality plus four explicit interface inputs: direct-route admissibility or
packet-frame fallback, finite-part packet identity, nonnegative common side
deficit, and exact assembly compatibility.

The four-interface-input status ledger is recorded in
`docs/four_interface_inputs_status_ledger.md`: direct-route admissibility is
conditionally closed in the explicit polylog regime, while the live center is
now the finite-part packet identity, nonnegative common side deficit, and
exact assembly compatibility; if kernel-length admissibility fails, the
Unified Packet-Frame Source theorem returns as the analytic prerequisite.

The finite-part packet identity arithmetic requirement is recorded in
`docs/finite_part_packet_identity_arithmetic_requirement.md`: the remaining
PGS-side obligation is a global completion-compatible first-moment law for
the deconvolved prime-power packet load, requiring a PGS-native counterterm,
chamber-centered von Mangoldt finite-part law, transport-reservoir matching
law, or a strengthened packet-frame theorem with an exact first-moment
component.

The reciprocal endpoint counterterm finite-part route is recorded in
`docs/reciprocal_endpoint_counterterm_finite_part_route.md`: it fixes the
explicit PGS-native counterterm
`C_X(z)=sum_{q<=X} log(q) J_z(0.5 log(q/p(q)))`, reduces its leading finite
part to reciprocal endpoint occupancy with value `C_eg/(2z)`, and isolates
the remaining nonlinear endpoint-kernel remainder. The square-gap energy
`sum g(q)^2 log(q)/q^2` supplies the leading endpoint term; full-kernel
control requires the nonlinear remainder finite part, for example from the
cubic reciprocal gap-energy `sum log(q) g(q)^3/q^3`, together with the
interior prime-power remainder law and gamma finite-part match.

The reciprocal square-gap energy lemma is recorded in
`docs/reciprocal_square_gap_energy_lemma.md`: the condition
`sum g(q)^2 log(q)/q^2 < infinity` implies the endpoint log-gap law
`G(X)=0.5 log(X)^2+C_eg+o(1)` and hence the leading finite part
`C_eg/(2z)`. The note reduces this condition to the Zero-Excess Return
Square-Moment Theorem, with the age-divisor energy bound
`sum (n-p(n))(tau(n)-2) << X log^B X` as the strongest current PGS-source
route; existing GWR and reciprocal-transport inputs do not yet prove that
global return-time frequency theorem.

The age-divisor reduction is recorded in
`docs/age_divisor_zero_excess_square_moment_reduction.md`: a dyadic bound on
`sum (n-p(n))(tau(n)-2)` implies
`sum_{X<q<=2X} (q-p(q))^2 << X log^B X` because each chamber of width `g`
carries age-divisor cost at least `g(g-1)/2`. The remaining proof obligation
is the dyadic upper bound for total age-divisor persistence, obtainable from
divisor-channel age orthogonality, a covering-tail theorem, or a GWR
persistence-transfer theorem.

The divisor-channel energy reduction is recorded in
`docs/divisor_channel_age_orthogonality_energy_bound.md`: the uniform bound
`sum_{X<n<=2X,d|n} (n-p(n)) << (X/d) log^B X` for
`2 <= d <= sqrt(2X)` implies the Age-Divisor Energy Bound after summing
small proper divisor channels, using `tau(n)-2 <= 2 * #{d|n:
2<=d<=sqrt(n)}`. The remaining gate at this level is the global
divisor-channel recurrence theorem: every divisor channel must carry only
polylogarithmic average zero-excess age on its natural dyadic population.
